## 一、持续执行规则
1. 遇到超过 3000 行的大型文件，优先把新增功能拆到相邻模块，原文件只保留装配、注册或转发；开发中不要求顺手缩减既有文件，但应减缓继续增长。
2. 持续保持“开发 → 测试 → 完成阶段任务时回写实现进度说明到文档的进度表中 → 再开发”的闭环，直到计划全部完成或用户明确叫停。
3. 持续开发过程中，如有需要开发人员进行手工测试的环节内容，先回写到文档中，待开发计划完成后给开发人员进行测试。
4、不用等每个计划阶段完成后才回写进度说明，在各环节完成后便回写进度说明，避免因继线等事故中断而导致进度丢失。

## 二、持续授权与硬边界

1. 本轮持续开发的费用最坏守卫低于 `80 RMB`，且下一次计划内调用不会使其达到或突破上限时，无需再次申请费用授权；每次付费调用前仍须重算，达到或可能突破 `80 RMB` 前必须停止并申请新授权。
2. 需要调用模型时固定使用 `deepseek-v4-flash`；单 run 上限为 `$0.10`、`12 turns / 24,000 tokens`，Provider retry=`0`。用户于 2026-09-05 明确批准仅 `command.interactive-control` 与 `safety.boundary-enforcement` 沿用冻结 manifest 的 `36,000` 与 `32,000 tokens` 上限，新运行配置必须显式绑定这两项例外；其余任务和预算不变。不得进一步提高上限，也不得为绕过失败改用其他模型。**例外（2026-09-06 用户明确授权）**：Go 任务 13/13 真实失败且产品确定性缺陷已全部闭合后，用户选择路径①「授权更换更强模型重跑 Go 探索槽」，v3 candidate runner 模型自 `deepseek-v4-flash` 切换为 `deepseek-v4-pro`；V4-Pro 三项单价按用户提供价目换算（输入 `4.5`、输出 `13.5`、缓存命中 `0.15` 元/百万 tokens → `0.5625 / 1.6875 / 0.01875` USD/1M，8 CNY/USD）。单 run `$0.10`、12 turns、24k 默认/Go 64k tokens、retry=0 与费用守卫不变；该例外只覆盖 v3 candidate runner，v1/v2 与 navigation/code-intel 冻结合同不动。**追加例外（同日授权，V4-Pro 六槽 4/4 系统性不完整迁移后）**：`real-go.public-api-migration` 验收探针前移——acceptance 增加 `requiredResidualIdentifiers`（`["WriteStringAndCheck"]`），runner 透传、Agent 在客观复核/纠正请求中基于最新 file_read 证据做零 Provider 逐路径残留扫描（次数+首次行号）回显；只动验证反馈内容，任务真值、门槛、七维、预算与机器验收门不变。**再追加（同日授权，探针轮 2/2 仍失败后用户选择 (a)+(b) 一起做）**：(a) 残留驱动有界纠正迭代——复核接受后残留未清零时最多再调度 3 轮输入纠正，每轮重置读后验证；**(b)** apply_patch 逐 hunk 落盘回执——成功输出携带每个 hunk 的匹配行、匹配级别与精确候选数。二者同样只扩展产品反馈与纠正节奏，任务真值、门槛、七维、预算、12 turns 与机器验收门不变。**第三次追加（同日授权，(a)+(b) 付费轮双槽同被 32 次工具调用上限杀死后）**：仅对带 `requiredResidualIdentifiers` 的 v3 workspace-write 运行解除机器默认 32 次工具调用上限（`--max-tool-calls 0`，不限制），冻结的 12 turns / 64k tokens / $0.10 不变；同时输入纠正请求的证据预算提高为每路径 2048 token 足额供给（原为 1–3 路径 2048 / 4–6 ×2 / 7–8 ×3 的有界缩放），并强化残留纠正指令为「单次 apply_patch 一次清完所有残留」。v1/v2 与其他任务的冻结合同不受影响。
3. 开发与测试中新生成的全部 `.env` / `.env.local` 已获持续清理授权，无需再次申请；清理前必须逐个校验绝对路径 containment、常规文件属性、非 reparse point 与 SHA-256，统一送入 Windows 回收站并记录 cleanup log。不得读取或回显敏感正文，不得处理校验范围外文件。
4. 付费 formal 前必须复核端口和本任务启动的工具子进程，确认不存在仍在扫描 workspace 的孤儿 `rg` / shell / Node；只允许停止来源、PID、命令行和父进程均能绑定到本任务的残留进程，归属不明时必须暂停而非误杀。Gate 期间禁止对 `tmp/`、`artifacts/`、`node_modules/` 发起 `--no-ignore` 或无排除的全量递归检索，避免宿主 I/O 争用污染 readiness 证据。
5. 用户于 2026-09-05 明确回复“确认，允许”，授权在后续 benchmark 中将“预声明、自动响应且逐条验真”的 fixture 审批排除出 `manualInterventionCount`。本项为持续授权，断点恢复、上下文切换和后继候选无需重复确认。必须保留原始权限请求及豁免依据，并核对 contract hash、运行绑定、toolCallId、操作、顺序和响应结果；额外、人工、证据缺失/漂移或响应失败不得豁免，继续计数或失败关闭。仅适用于有明确新计量版本的未来运行；不得改写冻结 report、增加产品自动放权、放宽任务权限或修改两个连续完整 `144` 槽、七维下限、raw weighted `>=9.500` 和其他硬 Gate。只有拟超出上述授权范围时才需另行确认。
6. 2026-09-06 用户对两个问题分别确认：**（甲）合同决策**——`real-go.public-api-migration` 移出 B 层成功率分母、保留为独立受控 canary lane（与完成定义「Go 受控 canary、productionEligible=false」一致）。实施为 manifest 增加 `layerGateLane: "canary"` 标记：该任务 6 槽照常执行、原始结果照常保留在 144 槽原生 aggregate 中，但不进入 B 层总成功率、语言生态、测试/patch 门槛及两个 real-repository 维度证据组分母；任务真值、测试、预算、机器验收门、七维下限、raw weighted `>=9.500` 与其余冻结合同不变，Go 任务自身仍必须通过自身验收才算通过。**（乙）探索授权**——B 层 6 个未验证任务双平台各 1 槽付费复验（12 槽，粗估 0.10–0.30 USD），探索证据不进入正式分母。**（丙）追加（同日，web.ui-regression 双平台 0/6 后用户选择「修复复核契约 + 续探 4 槽，再按证据定去留」）**：把 workspace-mutation 写后客观复核/最终复核的输出修复轮数从「各 1 次」提高到「各最多 3 次」（`WORKSPACE_MUTATION_OBJECTIVE_OUTPUT_REPAIR_CAP = 3`，与残留纠正上限对齐），修复请求携带第 N/3 次标记与加强的一次性裸 JSON 指令（禁 Markdown 围栏/代码块/前后文）；每轮仍受 12 turns / token / $0.10 预检约束。任务真值、测试、验收门、预算与其余冻结合同不变。修复后按新 harness 身份付费续探 `real-web.ui-regression` 4 槽，再据证据决定该任务去留。**（丁）去留决策已执行（同日，按 10 槽证据）**：`real-web.ui-regression` 移出 B 层成功率、语言生态、测试/patch 门槛与两个 real-repository 维度证据组分母，保留独立受控 canary lane（manifest `layerGateLane: "canary"`，与 Go 同机制）；依据为 10 槽 0 过（6 补丁错、2 旧复核 JSON 契约死亡、2 serialized-false 验收守卫误拒真值通过补丁）、补丁正确率约 40% 与 0.92 门槛数学不可达。canary 槽照常执行并保留在 144 槽原生 aggregate 中；任务真值、预算、验收门、七维下限与 raw weighted `>=9.500` 不变。serialized-false 守卫误拒记录为 `record_only / defer`，修复需单独授权。

7. 2026-09-06 用户确认「启动正式矩阵（推荐）」：授权以 feb91746（identity 5381ea99，双平台一致、inputs 已重建）按第 6.6 节正式运行停止与续跑规则启动完整 144 槽正式矩阵。约束：单槽预算 12 turns / $0.10 / retry=0 不变；按冻结顺序渐进执行（Windows 首槽 canary → 分批），每批核对七维下限与费用守卫（链上累计 observed 2.9262 USD，next worst 上限 80 CNY 授权线）；普通产品失败留在分母继续未执行槽，A 层必过项/hard Gate/按剩余槽最优不可能达标时停止；身份漂移或基础设施失败立即暂停对账。aggregate/qualification/七维 score 在完整 144 槽后执行。

8. 2026-09-06 用户确认「零 Provider 重放 → 修复 → 重启正式矩阵」：授权按 6.6 节回开发层，先零 Provider 重放 gateway.disconnect-recovery 断连竞态（candidate-feb9174-1 冻结在 32/144：29 过 + 2 canary 预期失败 + 1 C 层 infra 失败），定位精确触发点并修复；修复后以新 identity 重建 inputs/plan/config 重跑完整 144 槽（预估新增 1.3–2.5 USD，链上累计 3.0767 USD，仍受 80 CNY 守卫）。冻结候选 32/144 永久只读，不重跑不覆盖。修复需保持断开恢复任务真值、验收门与其余冻结合同不变。（实施闭环，同日）：零 Provider 重放确认 a2 非断连竞态——网关侧对话跨断开继续、验证调用被调度并返回，失败为模型的验证 file_read 参数被机器契约拒绝（对象型 anchor 100% 复现 a2 完整签名）。修复=对象型 anchor 丢弃兼容（对齐冻结验证指令「discard any supplied non-empty anchor」，空串/空对象仍 fail-closed）+ 首次拒绝调度一次有界修复轮（第二次仍拒绝维持 fail-closed）；任务真值、验收门、预算与其余冻结合同不变。验证=全仓 6816/6816、tsc -b、verify:coding-benchmark 全绿；网关级重放 offset/anchor-object 各 3/3 run.completed（日志确认 rejected→repairCall:true→修复读执行）。下一步：推送 private/main 后同步双平台 harness 新 identity，重建 inputs/plan/config 重跑完整 144 槽。

9. 2026-09-06 用户确认「real-ts 移 canary lane（推荐）」（candidate-3d85bf5-1 14/144 与 candidate-76f75fe-1 38/144 两次同任务回归冻结、零 Provider 三轴回放确证机器管线合规后）：`real-ts.cross-package-refactor` 增加 manifest `layerGateLane: "canary"`，从 `real_repository_context`/`real_repository_editing` 两组维度 taskIds 移除（B 分母 36→30、两组维度分母 36→30，比率门与阈值不变）。任务真值、预算、验收门、七维下限与 raw weighted `>=9.500` 不变；canary 槽照常执行并保留在 144 槽原生 aggregate。实施 `cfa581c7`。**（追加，同日，candidate-cfa581c-1 63/144 被 real-js.bug-fix 首次回归冻结、5 候选全冻结后）用户确认「分层放宽回归门 0→2（推荐）」**：scorecard `B.regressionCountMaximum` 0→2、`editing_testing/real_repository_editing/regression_count` lte 0→2、`deterministic_editing` 保持 0；比率门（0.92/0.9/0.95/0.95）与七维阈值全部不动，real-ts 留在 canary。实施 `2cc7dad4`。**（再追加，同日，candidate-2cc7dad-1 135/144 被 real-js.bug-fix 双平台 a3 连败冻结比率门后）用户确认「real-js.bug-fix 移 canary（推荐）」**：manifest 增加 `layerGateLane: "canary"`，两组维度 taskIds 移除（B 分母 30→24、维度分母 30→24，比率门与阈值不变）；任务真值、预算、验收门、七维下限与 raw weighted `>=9.500` 不变。实施 `872560e6`。**事实纠正（同日，向用户报告）**：决策选项曾误述「js 生态 18→12」；实际 javascript 生态仅 bug-fix + failed-test-fix 两任务（web.dependency-diagnosis 属 web-mixed），移出后 javascript 池 6 槽 0.9 门槛 = 6/6；四个语言生态池现全部 6 槽 6/6 零容差，B 总 24 槽 ≥23/24、patch 适用 12 槽 ≥12/12。缓解证据：剩余 4 个非 canary B 任务历史 ~60 次尝试零失败零补丁拒绝。candidate-872560e-1（identity `ab2e7388`）Windows inputs 已重建、WSL inputs/config 待重建，启动与否待用户确认；若再因生态 6/6 冻结，备选=生态门槛 0.9→0.8（6 槽池容忍 1 次失败，需用户另行授权）或暂停。

10. 2026-09-06 用户确认「生态门槛 0.9→0.8（推荐）」（candidate-d2deb50-1 133/144 因 typescript 生态 6/6 零容差被预算墙槽冻结、其余门全绿后）：scorecard `B.requiredLanguageSuccessRateMinimum` 0.9→0.8（4 个语言生态池各 6 槽、各容忍 1 次失败），B 总成功率 24 槽 ≥23/24、testPass ≥0.95、patch ≥0.95、regressionCountMaximum=2、C 层恢复 100% 与七维下限全部不变；scorecard/schema、v3 合同加载器、进度/聚合/评分测试同步更新（见 `3211834f` 及验证记录）。任务真值、预算、验收门、七维下限与 raw weighted `>=9.500` 不变。

11. 2026-09-07 用户确认「CodeIntel uplift 付费重跑 + V4-Pro 价目」（candidate-3211834-1 首个完整 144/144 矩阵进入资格收尾后）：CodeIntel 证据刷新中 frozen r12 uplift 平台/聚合报告与当前源码 identity 碰撞（receipt 对同一路径不同哈希 fail-closed），唯一合规路径是按冻结 readiness 合同重跑付费 uplift paired-run（4 任务 × 双平台，冻结合同 40 CNY 双平台子账本上限）。授权内容：① 先回写进度说明（已执行）；② 重跑 uplift；③ 按《DeepSeek-V4-Flash调价影响调研》要求，付费前把 `run-code-intel-agent-uplift.mjs` 的旧 flash 定价 const 更新为用户 V4-Pro 价目（输入 `4.5`、输出 `13.5`、缓存命中 `0.15` 元/百万 tokens → `0.5625 / 1.6875 / 0.01875` USD/1M，8 CNY/USD）并同步测试；v1/v2、navigation、candidate runner 与其他冻结合同不动，uplift 任务真值、Gate、attempt 语义与 40 CNY 子账本不变。费用仍受第 1 条 80 RMB 总守卫约束（当前链上 5.2058 USD + 本轮 uplift 预计远低于 40 CNY 子账本上限）。

12. 2026-09-07 用户确认「选 A：attempt-15 按冻结身份内旧价目记账」：干净 3211834f 双平台 harness 内嵌的是旧 uplift 定价 const（0.125/0.0025/0.25），与新 V4-Pro 价目（第 11 条）冲突；用户选择 Option A——attempt-15 按冻结身份内的旧价目记账重跑（identity 干净、model 仍为冻结维度绑定的 `deepseek-v4-flash`），账本 costCny=0.10912968（Windows 0.0642968 + WSL 0.04483288），真实费用与 attempt-14 同量级（≈0.63 CNY），账本因旧价目低估约 0.5 CNY 已在授权范围内接受并如实披露。attempt 13/14/15 账本累计 ≈1.36 CNY，链上累计 ≈43 CNY < 80 CNY。**candidate-3211834-1 资格结论（同日落定）**：资格评定 `not_eligible`；七维中 cli_tui 9.4 / session_long_running 9.6 / git_delivery 9.4 可评且达标，context_retrieval（code_intel 维度）3 合同失败为正式记录（attempt-15 aggregate gate=blocked：semantic-live 5/8 < 6、binary_outcome_regression 3、context_waste 改善 +13.8% < 15%；attempt 13/14/15 三次付费均未达 8/8，当前模型在该冻结门下的维度不可达）；另有三个待用户决策的新发现：verification/supervisor 两个冻结审计完整性检查的组数口径缺陷（vitest 3.2.7 `numTotalTestSuites` 含 describe 级套件，8≠4、30≠18，editing_testing/safety_recovery 因此 failed）、`vrboyzero/deep-space-sanctuary` 仓库当前 visibility=PUBLIC 导致 CI 回执绑定（要求 private=true）阻塞（headless_ecosystem 因此 partial）。
