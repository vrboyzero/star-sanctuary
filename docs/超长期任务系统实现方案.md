# Star Sanctuary 超长期任务系统实现方案

## 1. 目标

本文档定义 Star Sanctuary 面向“中大型项目 / 超长流程 / 多天至多周任务”的目标驱动执行系统。目标不是替代现有 `methods`、`memory`、`sub-agent`、`canvas`，而是把这些已实现能力组织成一套统一的长期任务操作系统。

这套系统需要解决四个核心问题：

1. 用户给出一个大目标后，系统能先把目标补全，而不是直接进入零散执行。
2. 系统能把目标沉淀为一整套规范化文档与可追踪状态，而不是只留在会话上下文里。
3. 主 Agent 在执行每个阶段时，必须围绕当前目标动态规划所需的方法、模组、子代理、工具、Skill、MCP，而不是被静态工具集限制。
4. 系统需要在超长周期下保持可恢复、可审计、可复盘、可沉淀。

---

## 2. 现状评估

Star Sanctuary 已经具备超长期任务系统的关键底座，但这些能力目前还是分散的：

- `methods/` 已支持 `method_list`、`method_search`、`method_read`、`method_create`，适合沉淀 SOP。
- `Task Memory` 已支持任务开始/结束捕获、工具调用记录、artifact 记录、memory links、任务摘要。
- `experience_candidates` 已支持从历史任务自动/手动生成 method/skill 候选并发布。
- `SubAgentOrchestrator` 已支持子 Agent 并发、排队、超时、深度限制与生命周期钩子。
- `canvas_*` 已支持画布式任务/知识可视化，并可绑定 method、memory、session、file。
- `Context Compaction`、`Auto Recall`、`Memory Viewer` 已解决长上下文和历史回溯问题。
- `Cron`、`Heartbeat`、`MCP`、`Skills`、`AgentRegistry` 已提供自动化执行和能力扩展基础。

当前缺口主要有六个：

1. 缺少“目标级容器”。现在有任务级记录，但没有长期目标级对象。
2. 缺少统一文档流水线。目标方案、设计方案、架构方案、拆分计划、Task List、`NORTHSTAR.md` 还没有统一生成与维护规范。
3. 缺少机器可执行的 DAG 任务图。现有 Task Memory 更像执行回执，不是长期计划引擎。
4. 缺少目标级追踪视图。当前任务、里程碑、阶段阻塞、审批点还未统一可视化。
5. 缺少“能力规划层”。主 Agent 还没有围绕节点目标显式规划 methods / skills / MCP / 子 Agent / 工具缺口。
6. 缺少长期任务状态机。现在已有 task capture，但没有“从目标初始化到阶段完成再到结项沉淀”的总流程。

结论：本项目不需要再新造一套平行系统，而应在现有 `memory + methodology + sub-agent + canvas + hooks` 基础上增建“目标层 + DAG 层 + 文档层 + 治理层”。

---

## 3. 设计原则

### 3.1 目标优先

所有规划、执行、调度、复盘，都必须围绕“当前目标是否更接近完成”判断，而不是围绕“当前有哪些工具”判断。

### 3.2 外部状态优先

超长期任务的关键状态不能只存在于模型上下文，必须外部化为文档、任务图、进度日志、画布、记忆记录。

### 3.3 复用优先

优先复用现有能力：

- SOP 复用 `methods/`
- 历史经验复用 `Task Memory` 与 `experience_candidates`
- 并行执行复用 `SubAgentOrchestrator`
- 可视化复用 `canvas`
- 长上下文稳定性复用 `Context Compaction`

### 3.4 双层表达

长期任务同时维护：

- 人类可读层：Markdown 文档、任务拆解清单、进度日志、`NORTHSTAR.md`
- 机器可执行层：`tasks.json`、goal state、checkpoint state、run metadata

### 3.5 动态能力规划

每个任务节点执行前，主 Agent 都要先判断：

- 现有方法是否可复用
- 现有 Skill / MCP / 工具是否足够
- 是否需要子 Agent
- 是否需要临时生成新方法、Skill 草稿、MCP 配置或执行模组

### 3.6 风险分级与 HiTL

涉及部署、数据库写入、敏感配置、破坏性文件操作、主版本依赖升级、外部发布等节点，必须进入审批断点。

---

## 4. 总体架构

建议将超长期任务系统拆成五层：

### 4.1 Goal Layer（目标层）

负责把用户需求转化为结构化目标对象，包含：

- 目标描述
- 边界与非目标
- 约束
- 成功标准
- 阶段划分
- 风险矩阵
- 审批点

### 4.2 Blueprint Layer（蓝图文档层）

负责输出和维护任务全周期文档：

- 目标方案
- 完整设计方案
- 项目架构与实现方案
- 实现方案拆分计划书
- 可追踪 Task List
- `NORTHSTAR.md`

### 4.3 DAG Execution Layer（执行图层）

负责把长期任务拆成机器可调度的 DAG 节点，并管理：

- 节点依赖
- 节点状态
- 节点负责人
- 能力规划
- 验收标准
- 回滚方案
- 里程碑推进

### 4.4 Memory & Experience Layer（记忆沉淀层）

复用现有 MemoryManager 体系，扩展为目标级闭环：

- Task Memory 记录每次节点执行结果
- methods 记录稳定流程
- experience candidates 记录可复用 method/skill 草稿
- memory chunks 保存项目知识、决策与复盘

### 4.5 Governance & Visualization Layer（治理与可视化层）

负责：

- 目标面板
- 任务图/阶段图
- 里程碑与阻塞展示
- 审批点管理
- 进度日志
- 画布联动

---

## 5. 标准文档体系

考虑到未来会同时存在多个不同的超长期任务，且这些任务不应绑定在当前项目仓库目录下，建议把“正式文档”和“运行态文件”统一收敛到状态目录 `~/.star_sanctuary/` 下，由状态目录承载多任务并存。

同时建议采用“默认路径 + 每个 goal 可单独指定路径”的混合模式：

- 默认情况下，长期任务运行态根目录放在 `~/.star_sanctuary/goals/<goal-id>/`
- 用户可为某个长期任务单独指定 `goalRoot`，例如放到 `E:\`、`D:\` 或某个项目自己的工作目录下
- 该路径既要写入 `NORTHSTAR.md` 供人类查看，也必须写入机器可读注册表供系统恢复

建议为每个长期目标建立一个独立工作目录：

```text
~/.star_sanctuary/docs/long-tasks/<goal-slug>/
├── 00-goal.md
├── 01-target-solution.md
├── 02-design.md
├── 03-architecture-and-implementation.md
├── 04-split-plan.md
├── 05-task-list.md
├── NORTHSTAR.md
├── tasks.json
├── progress.md
└── handoff.md
```

各文件职责如下：

- `00-goal.md`
  - 用户原始目标
  - 需求完善结果
  - 边界 / 非目标
  - 成功标准
  - 风险与约束

- `01-target-solution.md`
  - 最终目标方案
  - 方案选择理由
  - 备选方案与放弃原因

- `02-design.md`
  - 设计方案
  - 接口契约
  - 数据流
  - 验收口径

- `03-architecture-and-implementation.md`
  - 架构分层
  - 模块职责
  - 实现路径
  - 阶段目标

- `04-split-plan.md`
  - 类似“拆分计划书”
  - 每阶段实施步骤
  - 每阶段回归方案
  - 风险与回滚

- `05-task-list.md`
  - 人类可读任务清单
  - 与 `tasks.json` 一一映射

- `NORTHSTAR.md`
  - 本长期任务的局部宪法
  - 当前目标、约束、非目标、质量门槛、执行纪律
  - 当前 `goalRoot`、文档路径、运行态路径、artifact 路径等路径声明

- `tasks.json`
  - 机器可执行 DAG 任务图

- `progress.md`
  - 时序进度日志
  - 里程碑、阻塞、决策、下一步

- `handoff.md`
  - 中断恢复与交接摘要

建议同时新增长期任务注册表与目标级运行时目录：

```text
~/.star_sanctuary/goals/
├── index.json
└── <goal-id>/
```

其中：

- `index.json` 作为机器可读注册表，维护 `goalId -> goalRoot / northstarPath / status / lastActive` 等映射
- `<goal-id>/` 是默认运行态目录；若用户为某个 goal 指定了自定义路径，则注册表指向该路径

建议的默认运行时目录结构：

```text
~/.star_sanctuary/goals/<goal-id>/
├── state.json
├── checkpoints.json
├── runtime.json
├── board-ref.json
├── runs/
└── artifacts/
```

说明：

- `~/.star_sanctuary/docs/long-tasks/` 存放正式文档，但它属于状态目录而不是仓库目录。
- `~/.star_sanctuary/goals/` 是长期任务默认根目录，同时持有全局注册表 `index.json`。
- 默认情况下运行态、检查点、执行缓存和 artifact 映射都在 `~/.star_sanctuary/goals/<goal-id>/` 下。
- 若用户为某个长期任务指定了自定义 `goalRoot`，则该 goal 的实际运行态可迁移到外部路径，但注册表仍保留在 `~/.star_sanctuary/goals/index.json` 中。
- 一个 goal 一套独立目录，便于多个长期任务同时存在、暂停、恢复、归档。
- 仓库内只保留“系统实现方案”这类平台级文档，不默认承载具体长期任务实例文档。
- 若某个长期任务最终需要沉淀为项目交付物，可在结项时选择性导出到仓库，而不是默认直接写入仓库。
- `NORTHSTAR.md` 需要同步展示路径信息，但不应作为路径配置的唯一真源。

---

## 6. 核心数据模型

### 6.1 Goal

建议新增长期目标实体 `LongTermGoal`：

```json
{
  "id": "goal_app_modularization",
  "slug": "app-modularization",
  "title": "app.js 模块化重构",
  "status": "planning",
  "objective": "将 app.js 拆分为多模块并保持行为一致",
  "nonGoals": [
    "不引入新框架",
    "不改动既有协议"
  ],
  "constraints": [
    "保持 WebChat 可运行",
    "优先渐进式重构"
  ],
  "successCriteria": [
    "模块拆分完成",
    "关键回归通过"
  ],
  "currentPhase": "design",
  "goalRoot": "E:\\project\\sample\\.star_sanctuary-goals\\goal_app_modularization",
  "pathSource": "user-configured",
  "registryPath": "~/.star_sanctuary/goals/index.json",
  "docRoot": "~/.star_sanctuary/docs/long-tasks/app-modularization",
  "northstarPath": "~/.star_sanctuary/docs/long-tasks/app-modularization/NORTHSTAR.md",
  "tasksPath": "~/.star_sanctuary/docs/long-tasks/app-modularization/tasks.json",
  "progressPath": "~/.star_sanctuary/docs/long-tasks/app-modularization/progress.md",
  "runtimeRoot": "E:\\project\\sample\\.star_sanctuary-goals\\goal_app_modularization",
  "boardId": "goal_app_modularization_main",
  "createdAt": "2026-03-20T00:00:00.000Z",
  "updatedAt": "2026-03-20T00:00:00.000Z"
}
```

字段说明补充：

- `goalRoot`
  - 当前长期任务的实际根目录，允许是默认路径，也允许是用户指定的绝对路径
- `pathSource`
  - 标识该路径来自默认规则还是用户手动指定，例如 `default` / `user-configured`
- `registryPath`
  - 全局注册表位置，系统恢复任务时先查注册表，再打开对应 `NORTHSTAR.md`

### 6.2 Task Node

建议新增目标级 DAG 节点实体：

```json
{
  "id": "task_3_2",
  "goalId": "goal_app_modularization",
  "title": "拆分 memory 面板逻辑",
  "phase": "implementation",
  "status": "ready",
  "priority": "high",
  "riskLevel": "medium",
  "dependencies": ["task_2_1"],
  "objective": "将 Memory Viewer 状态与渲染逻辑从 app.js 提取到独立模块",
  "acceptance": [
    "memory 面板可正常打开",
    "搜索、筛选、详情查看正常"
  ],
  "rollback": [
    "恢复 app.js 原逻辑",
    "移除 memory.js 引用"
  ],
  "capabilityPlan": {
    "methods": ["frontend-module-split.md"],
    "skills": [],
    "mcp": [],
    "tools": ["file_read", "apply_patch"],
    "agents": ["default"],
    "gaps": []
  },
  "artifacts": [
    "apps/web/public/memory.js"
  ]
}
```

### 6.3 Run Record

建议新增节点执行记录 `GoalTaskRun`，与现有 `TaskRecord` 关联：

- `runId`
- `goalId`
- `nodeId`
- `conversationId`
- `agentId`
- `startedAt`
- `finishedAt`
- `result`
- `summary`
- `toolCalls`
- `memoryLinks`
- `methodUsages`
- `skillUsages`
- `artifacts`
- `approvalState`

落地策略：

1. 第一阶段可以先把 `goalId/nodeId/checkpointId` 放入现有 `tasks.metadata`。
2. 第二阶段再补独立表，避免一开始改动过大。

---

## 7. 目标驱动执行模板

### 7.1 Phase A：目标初始化

用户提出长期目标后，系统先进入“完善需求”模式，输出 `00-goal.md`。

必须补全的内容：

- 目标定义
- 业务背景
- 边界
- 非目标
- 约束
- 风险
- 成功标准
- 审批点

这一阶段不直接开始编码。

### 7.2 Phase B：蓝图与设计冻结

系统顺序生成：

1. `01-target-solution.md`
2. `02-design.md`
3. `03-architecture-and-implementation.md`
4. `04-split-plan.md`
5. `05-task-list.md`
6. `NORTHSTAR.md`
7. `tasks.json`

此阶段结束后，长期目标才算正式立项。

### 7.3 Phase C：任务图初始化

系统根据 `04-split-plan.md` 生成 `tasks.json` 和 Canvas 画布：

- Markdown 清单面向人审阅
- JSON DAG 面向 Agent 执行
- Canvas 面向结构理解与阶段协同

### 7.4 Phase D：节点执行循环

每个可执行节点都遵循固定循环：

1. 读取 `NORTHSTAR.md`
2. 读取当前节点与依赖节点结果
3. 检索相关 `methods`
4. 检索相关 memory / recent tasks / experience candidates
5. 生成本轮 `capabilityPlan`
6. 判断由主 Agent 执行还是拆给子 Agent
7. 执行
8. 回归验证
9. 更新 `tasks.json`、`progress.md`、Canvas、Task Memory
10. 判断是否需要沉淀 method / skill 草稿
11. 上下文压缩与 handoff 更新

### 7.5 Phase E：里程碑与结项

每个阶段结束时，系统要输出：

- 阶段完成摘要
- 已达成目标
- 剩余风险
- 未完成事项
- 新增/更新 methods
- 关键记忆沉淀

结项时输出最终复盘，并将高价值经验转成 methods / skills。

---

## 8. 长期任务专用通道与隔离

这是本方案必须补强的一点：超长期任务虽然复用现有工具与能力，但它不能直接混入普通对话任务通道，否则会带来状态污染、记忆串线、工具误用和体验混乱。

### 8.1 独立入口

建议新增“超长期任务专用入口”，与普通任务分开：

- 普通任务入口：继续沿用现有 chat/task 流程，适合即时问答、一次性修改、短任务。
- 长期任务入口：显式创建或恢复 `goal session`，进入目标级执行模式。

建议新增两类入口能力：

- `goal_init` / `goal_resume`
- Web 端 Goal Dashboard 中的“开始长期任务 / 恢复长期任务”

原则：

- 不是所有任务都进入长期任务系统。
- 只有明确立项的长期目标才进入 goal channel。
- 用户中途中断后，恢复时必须回到原 goal channel，而不是普通聊天通道继续。

### 8.2 独立会话命名空间

建议为长期任务引入独立会话命名空间：

- 普通会话：保持现有 `conversationId/sessionKey`
- 长期任务会话：新增 `goal:{goalId}` 命名空间
- 节点执行会话：新增 `goal:{goalId}:node:{nodeId}:run:{runId}`

这样做的作用：

- 目标级上下文与普通聊天隔离
- 同一长期任务的多个节点可独立运行、独立恢复
- 多个长期任务可并行存在，互不污染
- Task Memory、Recent Tasks、经验候选都可以按 `goalId` 精确过滤

### 8.3 工具复用但策略隔离

现有方法、技能、子代理、MCP、builtin tools 仍然复用，但在长期任务通道里需要增加一层“goal-aware 策略包装”。

具体要求：

- `methods`
  - 普通任务照常使用
  - 长期任务中优先读取与当前 `goalId`、当前阶段、当前节点相关的方法
  - 长期任务产生的新方法默认先记到 goal 相关上下文，再决定是否晋升为通用 method

- `skills`
  - 普通任务维持当前发现与调用逻辑
  - 长期任务需要记录“本节点实际使用了哪个 skill”，并纳入 goal run

- `sub-agent`
  - 普通任务仍可临时委托
  - 长期任务中的子 Agent 必须继承 `goalId/nodeId/runId`
  - 子 Agent 输出要挂回当前节点，而不是散落成普通独立任务

- `memory`
  - 普通任务继续使用全局记忆
  - 长期任务默认增加 `goalId` 过滤与关联，优先召回当前目标相关记忆、阶段决策、历史节点结果

- `canvas`
  - 普通画布继续独立使用
  - 长期任务自动绑定一个主板，作为目标级可视化工作区

结论：复用的是能力本身，隔离的是入口、上下文、状态归属、记录维度和调度策略。

### 8.4 暂停与恢复机制

长期任务的暂停是常态，不是异常。

因此必须支持：

- 用户手动暂停 goal
- 节点执行后自动暂停，等待下一次恢复
- 因 checkpoint 进入 `pending_approval`
- 因阻塞进入 `blocked`
- 因资源不足进入 `idle`

恢复时，系统应从以下资料恢复上下文，而不是依赖模型记忆：

- `NORTHSTAR.md`
- `tasks.json`
- `progress.md`
- 最新 checkpoint
- 最新 handoff
- 当前节点最近一次 run 记录
- goal board 摘要

### 8.5 与普通任务的硬性边界

需要明确以下边界：

1. 普通任务不得默认写入长期任务文档目录。
2. 普通任务不得默认占用长期任务的 `goal session`。
3. 长期任务专用工具不应污染普通任务提示词与调用路径。
4. 长期任务的 checkpoint、progress、task graph 更新，不应出现在普通任务执行路径里。
5. 长期任务需要显式 opt-in，而不是隐式触发。

因此系统实现上建议采用：

- 独立 goal RPC / tool 前缀
- 独立 goal sessionKey 命名空间
- 独立 goal prompt 注入协议
- 独立 goal runtime 目录
- 独立 goal dashboard 入口

---

## 9. 能力规划层设计

这是本系统最关键的新层。

### 9.1 为什么必须新增能力规划层

用户已经明确提出：执行过程中不要被已有的方法、模组、子代理、工具、Skill、MCP 限制，而要以当前目标为核心做资源规划。

因此每个节点都要显式生成 `capabilityPlan`，而不是隐式依赖模型临场发挥。

### 9.2 capabilityPlan 应包含的内容

- 当前节点目标
- 已知可复用 methods
- 已知可复用 skills
- 已知可用 builtin tools / MCP / plugins
- 是否需要子 Agent
- 是否需要新建临时方法或模组
- 是否存在能力缺口
- 缺口的获取策略

建议策略顺序：

1. 先搜现有 `methods`
2. 再搜 `skills`
3. 再看现有 builtin tools / MCP / plugins
4. 再决定是否调用子 Agent
5. 仍不足时，生成临时执行模板或新能力草稿
6. 若涉及高风险扩展，再进入 HiTL

### 9.3 动态扩展原则

若发现现有能力不足，系统允许：

- 新增 method 草稿
- 新增 skill 草稿
- 新增 MCP 配置草案
- 新增临时子 Agent 角色
- 新增专门的节点级执行模版

但要求：

- 先解决当前节点目标
- 再判断是否值得沉淀为长期资产
- 新资产必须可审计、可回滚、可复用

---

## 10. 与现有实现的映射

### 10.1 `packages/belldandy-memory`

负责新增：

- `LongTermGoal` 存储
- `GoalTaskNode` 存储
- `GoalTaskRun` 存储
- goal-scoped 查询能力
- task 与 goal 的映射

同时复用现有：

- `tasks`
- `task_memory_links`
- `experience_candidates`
- `experience_usages`

### 10.2 `packages/belldandy-skills`

建议新增一组长期任务内置工具：

- `goal_init`
- `goal_get`
- `goal_update`
- `goal_list`
- `goal_resume`
- `goal_pause`
- `northstar_write`
- `task_graph_create`
- `task_graph_read`
- `task_graph_update`
- `task_graph_claim`
- `task_graph_complete`
- `task_graph_block`
- `progress_append`
- `checkpoint_request`
- `checkpoint_resolve`
- `orchestrate`

其中：

- `orchestrate` 对应当前待实施项，负责执行 DAG。
- `checkpoint_*` 负责 HiTL 断点。
- `task_graph_*` 负责长期任务图而非普通 Task Memory。
- `goal_*` 工具只在长期任务通道加载或优先暴露，避免干扰普通任务调用路径。

### 10.3 `packages/belldandy-agent`

建议新增“长期任务协议”注入：

- 当前目标摘要
- 当前阶段
- `NORTHSTAR.md` 关键约束
- 当前节点定义
- 本轮 `capabilityPlan`
- 可复用 method / skill 摘要
- 本轮验收标准

并扩展执行策略：

- goal channel 与普通 chat channel 分离
- goal resume 时优先读取外部状态文件而非依赖会话残留上下文
- 节点级上下文重置
- 节点完成后自动 handoff
- 节点失败后自动写入阻塞与建议
- 节点执行时自动选择主 Agent / 子 Agent / 并行模式

### 10.4 `packages/belldandy-core`

负责：

- Gateway 启动时加载 goal runtime
- 维护 `~/.star_sanctuary/goals/index.json` 注册表
- 注册长期任务相关 RPC / tools
- 维护 checkpoint 状态
- 通过 hooks 把 `goalId/nodeId` 注入 Task Memory
- 与 Cron / Heartbeat 打通
- 为长期任务建立独立 session namespace 与恢复入口
- 控制 goal tools 只在 goal channel 中启用或提升优先级

### 10.5 `apps/web/public`

建议新增前端视图：

- Goal Dashboard
- 阶段视图
- DAG / Task List 视图
- Checkpoint 审批视图
- Goal Progress 时间线
- Goal Canvas 入口
- Goal Resume / Pause 入口
- 当前普通聊天与长期任务模式切换提示

现有 Canvas 可以直接作为第一版可视化底板，不必重造新图编辑器。

---

## 11. 状态机设计

### 11.1 Goal 状态

建议：

- `draft`
- `aligning`
- `planning`
- `ready`
- `executing`
- `blocked`
- `pending_approval`
- `reviewing`
- `completed`
- `archived`

### 11.2 Task Node 状态

建议：

- `draft`
- `ready`
- `in_progress`
- `blocked`
- `pending_review`
- `validating`
- `done`
- `failed`
- `skipped`

### 11.3 Checkpoint 状态

建议：

- `not_required`
- `required`
- `waiting_user`
- `approved`
- `rejected`
- `expired`

---

## 12. HiTL 断点策略

长期任务系统必须预置断点，而不是临场判断。

建议至少在以下场景自动创建 checkpoint：

- 目标冻结
- 设计冻结
- 架构冻结
- 执行阶段进入高风险节点
- 部署前
- 数据迁移前
- 大范围文件删除前
- 主版本依赖升级前
- 面向外部系统写入前
- 结项前

checkpoint 内容应包含：

- 将执行什么
- 影响范围
- 回滚方案
- 风险等级
- 验证方式
- 是否阻塞后续 DAG

---

## 13. 记忆与沉淀闭环

长期任务系统不应只“完成任务”，还要把过程变成长期资产。

### 13.1 任务层

每个节点执行后写入 Task Memory：

- objective
- toolCalls
- artifactPaths
- memoryLinks
- summary
- reflection

### 13.2 方法层

以下情况优先沉淀 method：

- 某类节点执行两次以上
- 跨多个 goal 复用
- 有清晰步骤和常见失败经验

### 13.3 Skill / MCP 层

以下情况优先沉淀 skill / MCP：

- 明显需要可复用的操作模板
- 明显需要新的外部能力接入
- 可被多个目标多次使用

### 13.4 Goal 结项层

结项时应自动生成：

- goal retrospective
- reusable methods list
- reusable skills candidates list
- unresolved risks list

---

## 14. 分阶段实施路线

### Phase 1：文档与状态基础层

目标：

- 打通 `LongTermGoal`、`tasks.json`、`NORTHSTAR.md`、`progress.md`
- 先有“长期任务容器”，再谈自治执行

实施项：

- 新增 goal 数据模型
- 新增 goal runtime 存储
- 新增长期任务文档脚手架生成
- 文档根目录改为 `~/.star_sanctuary/docs/long-tasks/`
- 建立 goal channel / goal session 命名空间
- 给 Task Memory 注入 `goalId/nodeId`

验收：

- 能创建一个长期目标
- 能生成标准文档目录
- 能同时存在多个 goal 且互不串线
- 能暂停后恢复到原 goal session
- 能维护 `tasks.json`
- 能从 Task Memory 反查所属 goal / node

### Phase 2：DAG 执行与节点状态机

目标：

- 让目标真正可调度

实施项：

- [x] `task_graph_*` 工具
- [x] 节点依赖校验
- [x] 状态流转骨架
- [x] `progress.md` 自动追加
- [x] 基础 checkpoint 流程

验收：

- [x] 能创建并执行 DAG 骨架
- [x] 能阻止未满足依赖的节点启动
- [x] 能记录节点成功/失败/阻塞
- [x] 节点执行日志自动沉淀到 `progress.md`
- [x] checkpoint 审批流与节点状态联动

### Phase 3：目标驱动编排

目标：

- 让主 Agent 在节点执行前显式做能力规划

实施项：

- `capabilityPlan` 生成器
- `orchestrate` 工具
- 与 `SubAgentOrchestrator` 联动
- methods / skills / MCP 搜索与选用策略

验收：

- [x] 能针对节点自动判断单 Agent / 多 Agent
- [x] 能记录 methods / skills / MCP 的实际使用并回写到 `capabilityPlan.actualUsage`
- [x] 能识别能力缺口并形成补充建议
- [x] 高风险节点在 orchestrate 前可自动提升为 checkpoint 审批入口

### Phase 4：可视化与治理

目标：

- 让长期任务可被看见、被审阅、被恢复

实施项：

- Goal Dashboard
- Task Graph / Timeline / Checkpoint UI
- Canvas 自动同步
- handoff 生成

当前进度（2026-03-20）：

- [ ] Goal Dashboard
- [x] Task Graph / Timeline / Checkpoint UI
- [x] Canvas 自动同步
- [x] handoff 生成（最小版：`goal.handoff.generate` / `goal_handoff_generate` + `handoff.md` 自动写入 + WebChat goal detail 摘要卡片）
- [x] handoff 自动刷新（最小闭环：关键节点 / checkpoint 流转后自动重写 `handoff.md`）
- [x] WebChat goal detail 自动刷新（最小闭环：关键流转触发 `goal.update`，当前选中 goal 自动刷新 `handoff / tracking / progress`）
- [x] `goal.update` 前端轻量去抖与区域级刷新判定（避免高频流转时每次都整块 `renderGoalDetail()`）
- [x] `goal.update` 的 `reason -> areas` 已进入集中协议化治理（后端不再分散手写 `areas`，统一由协议表推导）

说明：

- 当前已补上 Phase 4 所需的最小 handoff 治理闭环，支持在长期任务执行中手动生成正式 `handoff.md`，并在 WebChat goal detail 中直接查看恢复摘要、下一步建议、阻塞项、open checkpoint 与最近时间线。
- 当前已进入“关键流转自动刷新 handoff”的最小版本：`claim / pending_review / validating / complete / block / fail / skip / checkpoint request/approve/reject/expire/reopen / pause / resume / capability plan save` 后，会在 manager 层自动重写 `handoff.md`。
- 当前已补上 WebChat 侧的最小自动刷新闭环：manager 在关键流转后广播 `goal.update` 事件，前端在当前选中 goal 命中时，直接刷新 detail 区域并重载 `handoff / tracking / progress`（同时同步最新 goal 摘要状态），不再依赖手动刷新或 `goal.list` 全量重载。
- 当前已进一步补上 `goal.update` 的前端收敛策略：同一 `goalId` 的连续事件先做轻量去抖聚合，再依据 `areas + 关键字段差异` 判定是“局部 panel 刷新”还是“整块 detail 重绘”，避免高频节点流转时反复整块 `renderGoalDetail()`。
- 当前已开始补上 `goal.update` 的协议治理收尾：`GoalUpdateReason / GoalUpdateArea / GoalUpdateEvent` 与统一 `reason -> areas` 协议表已集中定义，manager 后续关键流转只传 `reason`，不再在各个变更点散落手写 `areas`，以保证 Phase 5 自动化建立在稳定事件协议之上。
- 当前自动刷新策略是治理层 best-effort：默认不为每次自动刷新额外追加 `handoff_generated` 进度事件，避免 `progress.md` 被刷新噪音淹没；显式手动生成时仍会记录正式 `handoff_generated` 事件。
- 当前仍未进入“节点完成后附加更丰富 artifact 摘要 / review chain 交接摘要 / 完整 Goal Dashboard 汇总页”阶段，这些留待后续 Phase 4 补完。

阶段结论（2026-03-20）：

- 当前可认定为“Phase 4 核心闭环已完成，可进入 Phase 5”。
- 这里的“完成”是指：长期任务已具备最小可见、可恢复、可追踪、可自动刷新、可稳定广播事件协议的治理基础。
- 当前未完成项主要属于 Phase 4 增强项，而不是 Phase 5 前置阻塞项，例如：
  - 完整 Goal Dashboard
  - 更丰富的 handoff artifact 摘要
  - 完整 `review chain / quorum / escalation workflow`
- 因此后续开发应转入 Phase 5 主线，优先建设“经验沉淀自动化”的后端闭环；Phase 4 增强项作为并行延后项处理。

验收：

- 用户可查看阶段、阻塞、里程碑、审批点
- 中断后可从文档和状态恢复执行

### Phase 5：经验沉淀自动化

目标：

- 形成“目标执行 -> 经验抽取 -> method/skill 沉淀”的闭环

实施项：

- goal retrospective
- method candidate 批量建议
- skill candidate 批量建议
- 高频流程检测

当前进度（2026-03-20）：

- [x] P5-1 `goal retrospective` 生成器（最小版）
  - 已新增 `retrospective.json + 06-retrospective.md` 双输出
  - retrospective 当前聚合 `tasks.json / checkpoints.json / capability-plans.json / progress.md / handoff`，形成目标级复盘摘要
  - 已通过 `goal.retrospect.generate / goal_retrospect_generate` 暴露最小 RPC / tool 入口
  - 当前输出重点是目标完成度、checkpoint 摘要、capability 摘要、highlighted nodes、blockers、recommendations
  - 本轮未新增新的前端页面或 Dashboard，符合 `16.7` 的前端技术债预防策略
- [x] P5-2 method candidate 自动建议（最小版）
  - 已新增 `method-candidates.json + 07-method-candidates.md` 双输出
  - method candidate 当前基于 `retrospective + tasks.json + capability-plans.json + progress.md` 生成
  - 每条建议都保留 `goalId / nodeId / runId / artifacts / acceptance / methodsUsed / skillsUsed / mcpServersUsed / progressEvents / references`
  - 当前先落在 goal 侧建议层，不直接写入全局 `experience_candidates`，避免过早污染正式经验池
  - 已通过 `goal.method_candidates.generate / goal_method_candidates_generate` 暴露最小 RPC / tool 入口
  - 本轮仍未新增新的前端页面或 Goal Dashboard 入口，符合 `16.7` 的前端技术债预防策略
- [x] P5-3 skill candidate 自动建议（最小版）
  - 已新增 `skill-candidates.json + 08-skill-candidates.md` 双输出
  - skill candidate 当前基于 `retrospective + capability-plans.json` 聚焦 `gaps / multi-agent / tool chain / MCP / deviations`
  - 每条建议都保留 `goalId / nodeId / runId / executionMode / riskLevel / gaps / toolNamesUsed / mcpServersUsed / deviations / references`
  - 当前先落在 goal 侧建议层，不直接写入全局 `experience_candidates` 或正式 `skills/`
  - 已通过 `goal.skill_candidates.generate / goal_skill_candidates_generate` 暴露最小 RPC / tool 入口
  - 本轮仍未新增新的前端页面或 Goal Dashboard 入口，符合 `16.7` 的前端技术债预防策略
- [x] P5-4 高频流程检测（最小版）
  - 已新增 `flow-patterns.json + 09-flow-patterns.md` 双输出
  - 当前基于节点状态流转事件序列、`executionMode / checkpointMode / riskLevel / toolNames / MCP` 生成最小 flow signature
  - 会对同一 goal 内重复出现的 signature 聚合计数，并输出 `observe / promote_method / promote_skill / promote_both` 建议动作
  - 当前聚焦“goal 内模式识别”，尚未扩展到跨 goal 全局模式归并
  - 已通过 `goal.flow_patterns.generate / goal_flow_patterns_generate` 暴露最小 RPC / tool 入口
  - 本轮仍未新增新的前端页面或 Goal Dashboard 入口，符合 `16.7` 的前端技术债预防策略
- [x] P5-5 最小工具与 RPC 聚合入口（最小版）
  - 已新增 `goal.experience.suggest / goal_experience_suggest` 聚合入口
  - 当前统一聚合 `retrospective + methodCandidates + skillCandidates + flowPatterns + summary + recommendations`
  - 当前定位是“建议层聚合读取入口”，用于系统调用、后续只读展示和人工审阅，不引入新的前端壳层
  - 本轮仍未新增新的前端页面、独立长期任务 store 或平行数据链路，符合 `16.7` 的前端技术债预防策略

阶段结论（2026-03-20）：

- Phase 5 第一轮核心目标已完成：系统已能围绕单个 goal 自动产出 retrospective、method/skill 候选、高频流程摘要，并通过 tool / RPC 聚合读取。
- Phase 5 第一轮到此收口，不继续扩展为“自动发布到正式 `methods/` / `skills/`”或“跨 goal 全局经验池自动推广”。
- 若后续需要前端可见性，仅允许在现有 `goal detail / modal / canvas context` 内做最小只读接入；不在本轮新增 Dashboard、专属状态缓存或更复杂交互壳层。

最小实施原则（进入 Phase 5 时）：

- 优先做后端自动化与运行态沉淀，不优先扩前端交互。
- 严格遵守 `16.7 Phase 4 前的前端技术债预防策略`：Phase 5 不新增新的长期任务前端壳层，不扩散成新的 Dashboard 页面。
- 若 Phase 5 需要前端可见性，优先复用现有 `goal detail / modal / canvas context` 入口，只补最小只读展示，不先追求更完整交互体验。
- Phase 5 的第一轮验收以“自动产出结构化经验结果、可被工具链复用、可被后续人工审阅”为主，而不是以前端体验完整度为主。

验收：

- 结项后可自动产出可复用资产建议
- 后续类似目标可显著减少初始规划成本

### Phase 6：经验发布与跨 Goal 治理

目标：

- 形成“goal 侧建议层 -> 人工审阅 -> 正式 method / skill / experience 资产发布”的治理闭环
- 把单个 goal 内的经验抽取，提升为跨 goal、跨项目可复用的组织级资产沉淀能力

实施项：

- method / skill candidate 审阅与发布流
- goal 侧建议层到正式资产层的映射与追踪
- 跨 goal flow pattern 归并与排序
- reviewer / policy / SLA / escalation 治理能力补完

建议范围（Phase 6 第一轮）：

1. 建立 `goal suggestion -> review -> publish` 最小闭环
   - 为 `method-candidates / skill-candidates / flow-patterns` 增加正式审阅状态
   - 支持 `accept / reject / defer / needs_revision`
   - 发布后写入正式 `methods/`、`skills/` 或 `experience_candidates`
   - 保留从正式资产回溯到 `goalId / nodeId / runId / evidence` 的来源映射
2. 建立跨 goal 的经验归并能力
   - 在单 goal `flow-patterns` 之上，增加跨 goal 的 signature 归并与统计
   - 识别“在多个目标中重复出现”的方法模式、能力缺口模式与审批模式
   - 为发布优先级提供更稳定的排序依据
3. 补完治理层
   - reviewer 目录、默认 reviewer 继承、审批模板
   - `review chain / quorum / escalation workflow` 的正式工作流能力
   - SLA 超时提醒、人工改派、升级路径记录
4. 保持前端克制
   - Phase 6 第一轮仍不优先建设复杂 Dashboard
   - 若需要可见性，优先在现有 `goal detail / experience review modal / method candidate review panel` 中做最小可用接入
   - 在 `app.js` 正式模块化拆分前，不扩散成新的长期任务前端壳层

当前进度（2026-03-20）：

- [x] P6-1 `suggestion-reviews.json` 与 review 状态机骨架（最小版）
  - 已新增 `suggestion-reviews.json` 运行态文件
  - 已支持从 `method-candidates / skill-candidates / flow-patterns` 自动同步 review items
  - 已提供最小 review 状态：`pending_review / accepted / rejected / deferred / needs_revision`
  - 已提供最小 RPC / tool 入口：`goal.suggestion_review.list / goal.suggestion_review.decide` 与 `goal_suggestion_review_list / goal_suggestion_review_decide`
  - 当前阶段仍未进入正式 publish pipeline，仅解决 review state 骨架与来源映射
- [x] P6-2 review 通过后的 suggestion publish pipeline（最小版）
  - 已新增 `publish-records.json` 运行态文件
  - 已支持对 `accepted` 的 `method_candidate / skill_candidate` 进行正式发布
  - method 当前直接写入正式 `methods/` 目录；skill 当前复用现有 `publishSkillCandidate` 写入正式 `skills/` 目录
  - 已提供最小 RPC / tool 入口：`goal.suggestion.publish / goal_suggestion_publish`
  - 当前阶段仍未进入 `experience_candidates` 全局映射与去重治理，该部分保留给 P6-3
- [x] P6-3 goal suggestion 发布结果 -> 全局 `experience_candidates`（最小版）
  - 已支持在 suggestion 正式发布后，同步写入全局 `experience_candidates`
  - 当前复用现有 `MemoryManager / experience_candidates` schema，不新增第三套 candidate 池
  - 当前最小去重规则：使用稳定 synthetic `taskId = goal_suggestion:{goalId}:{suggestionType}:{suggestionId}`，并复用现有 `task_id + type` 唯一约束
  - 当前已保留 `publishedPath / reviewId / sourcePath / goal suggestion taskId`，保证全局 candidate 仍可回溯 goal 来源
- [x] P6-4 cross-goal flow pattern 归并（最小版）
  - 已新增跨 goal 只读归并器：扫描所有 goal 的 `flow-patterns.json`，按 `signature` 聚合
  - 已输出全局运行态与文档：
    - `~/.star_sanctuary/goals/cross-goal-flow-patterns.json`
    - `~/.star_sanctuary/docs/long-tasks/cross-goal-flow-patterns.md`
  - 已输出 `goalCount / occurrenceCount / recommendedAction / goalRefs`
  - 当前推荐动作策略已补“跨 2 个及以上 goal 重复出现时，若无更强动作票据，至少提升为 `promote_method`”
  - 当前阶段仍是全局只读归并，不回写单个 goal 运行态，也未进入完整 publish / review chain / quorum / escalation workflow
- [x] P6-5 review governance 最小入口（最小版）
  - 已补统一只读聚合入口：`goal.review_governance.summary / goal_review_governance_summary`
  - 当前已扩展聚合 `governance config + notifications + suggestion review + checkpoint workflow + publish records + cross-goal focus patterns + recommendations`
  - 已复用现有 `secureMethods / GOAL_TOOL_NAMES / goal-only` 能力注入与暴露范围控制
  - 当前定位是“治理总览入口”，用于 CLI / tool / 现有 goal detail 只读接入，不新增 Dashboard 或新的长期任务前端壳层
  - 当前已可作为统一审批治理入口继续承载 `review chain / quorum / escalation / reminder / notification` 的聚合可视
- [x] P6-6 review chain / quorum / escalation workflow 骨架（最小版）
  - 已为 `suggestion review` 增加 workflow snapshot：支持 `single / chain / quorum`
  - 已支持通过 `goal.suggestion_review.workflow.set / goal_suggestion_review_workflow_set` 配置最小审批工作流
  - 已支持通过现有 `goal.suggestion_review.decide / goal_suggestion_review_decide` 按 workflow 推进审批，而不是一律直接终态放行
  - 已支持通过 `goal.suggestion_review.escalate / goal_suggestion_review_escalate` 执行当前 stage 的手动 escalation
  - 已补 `goal.suggestion_review.scan / goal_suggestion_review_scan`：可扫描当前 goal 下已超 SLA 的 workflow stage，并按已配置 `escalationReviewer` 执行最小自动 escalation
  - 已补 reviewer 目录 / 组织级 reviewer template / reminder / SLA policy 运行态：
    - `~/.star_sanctuary/governance/review-governance.json`
    - `<goal.runtimeRoot>/review-notifications.json`
    - `<goal.runtimeRoot>/review-notification-dispatches.json`
  - 已补统一 `goal.approval.scan / goal_approval_scan`：统一扫描 `suggestion review + checkpoint workflow`，输出 overdue / auto escalation / reminder notifications
  - 已让 checkpoint 复用与 suggestion review 相同的 workflow kernel，支持 chain / quorum / escalation / SLA / reminder / org policy 继承
  - 已补 `goal.checkpoint.escalate / goal_checkpoint_escalate`
  - 已补更细粒度通知 dispatch / outbox 运行态：支持 `goal_detail / goal_channel / reviewer_inbox / org_feed / im_dm / webhook`
  - 已在现有 `goal detail` 内补统一 approval panel，展示 reviewer/template 摘要、pending/overdue、actionable reviews/checkpoints、recent notifications、dispatch channel / outbox 状态
  - 当前剩余次级缺口主要是：
    - 组织级 reviewer / template / SLA policy 的在线编辑面板

与当前边界的关系：

- Phase 5 已完成的是“建议生成与聚合读取”；Phase 6 才进入“正式审阅、正式发布、跨 goal 治理”。
- Phase 2 / Phase 4 的遗留增强项，如更完整的 artifact 摘要、完整 Goal Dashboard、更多可视化交接卡片，不应混入 Phase 6 主线；这些属于并行增强 backlog。
- 因此 Phase 6 的主线重点不是“看得更丰富”，而是“让经验资产真正进入可治理、可发布、可复用状态”。

建议实施顺序（Phase 6）：

1. method / skill candidate 的审阅状态机与发布映射
2. 资产来源追踪与发布记录
3. 跨 goal flow pattern 归并
4. reviewer 目录与审批模板
5. `review chain / quorum / escalation` 工作流
6. 统一 approval panel / reminder / SLA scan
7. 如有必要，再补治理配置在线编辑与常驻自动扫描

Phase 6 第一轮完成标准：

- 至少一种 candidate 可进入正式审阅状态
- 至少支持 method 或 skill 的人工批准后发布
- 发布结果可回溯到原始 goal / node / run / evidence
- 至少能输出一版跨 goal flow pattern 归并摘要
- 不因发布治理而新增新的长期任务前端壳层

明确不在 Phase 6 第一轮内的事项：

- 全自动无人工审核地发布正式 method / skill
- 大规模重做长期任务前端 Dashboard
- 把所有旧阶段增强项都塞进 Phase 6 主线
- 在 `app.js` 模块化拆分前进行前端体验大扩张

---

## 15. MVP 范围建议

第一版不要一口气做完整自治平台，建议 MVP 只覆盖以下能力：

1. 目标初始化与标准文档生成
2. `tasks.json` + `05-task-list.md` 双轨任务图
3. `goalId/nodeId` 注入 Task Memory
4. goal channel / pause / resume
5. 基础 checkpoint
6. 节点级 `capabilityPlan`
7. Canvas 自动创建长期任务主板

这 7 项完成后，Star Sanctuary 就已经从“会做任务的 Agent”升级为“会经营长期目标的 Agent”。

---

## 16. 关键决策

### 16.1 不新造第二套记忆系统

长期任务系统必须建立在现有 MemoryManager 之上，而不是再做一套独立记忆库。

### 16.2 不让长期任务只存在于对话

长期目标必须有文档、任务图、进度日志、画布、checkpoint。

### 16.3 文档与运行态统一进入状态目录

长期任务文档与运行态都进入 `~/.star_sanctuary/`，其中正式实例文档进 `~/.star_sanctuary/docs/long-tasks/`，运行态进 `~/.star_sanctuary/goals/`。仓库仅保留平台级方案文档，不默认承载具体长期任务实例。

### 16.4 长期任务与普通任务分通道

长期任务必须使用独立入口、独立 session namespace、独立 prompt 注入和独立运行态目录；允许复用工具与能力，但不允许与普通任务共用同一执行通道。

### 16.5 路径配置以注册表为准，`NORTHSTAR.md` 同步展示

每个长期任务都可以使用默认路径，也可以使用用户指定路径；真实路径配置应以 `~/.star_sanctuary/goals/index.json` 等机器可读注册表为准，`NORTHSTAR.md` 负责同步展示，不能作为唯一真源。

### 16.6 先目标级编排，再追求完全自治

当前项目已经具备很多自治能力，真正的短板不是“不会调用工具”，而是“没有目标级操作系统”。

### 16.7 Phase 4 前的前端技术债预防策略

当前 `apps/web/public/app.js` 已进入高风险体量区间。考虑到 Phase 4 / Phase 5 还会继续增加 `Goal Dashboard / Timeline / Checkpoint / Canvas 联动` 等复杂视图，如果没有前置约束，后续很容易把长期任务前端继续堆成不可维护的巨型单体。

因此这里做一个明确决策：  
**当前阶段不立即拆分 `app.js`，但从现在开始必须按“受控单体（controlled monolith）+ 模块就绪边界”的方式继续开发。**

这项决策对应 `docs/app.js的技术债处理建议.md` 中的 3 条建议，并将它们正式收敛为后续实施约束。

#### 16.7.1 当前阶段的总原则

- 当前阶段**不立即拆分** `app.js`，避免在超长期任务主链路尚未完全跑稳时过早重构。
- 当前阶段**不引入 React/Vue 等新框架**；后续如需拆分，只允许沿浏览器原生 `ES Modules` 路线演进。
- 当前阶段允许继续在 `app.js` 中开发，但新增代码必须写成**未来可直接抽离**的块，而不是继续扩大无边界全局脚本。

#### 16.7.2 后续前端开发红线

- 不再新增无命名空间的顶层全局 `let/const` 状态变量。
- 新状态只能挂入既有状态容器，或新增清晰命名的单一命名空间对象，例如 `goalsState.*`、`memoryViewerState.*`。
- 不允许把长期任务相关逻辑零散插入 `app.js` 多个不相邻区域；同一功能必须尽量集中到一个连续代码块。
- 不允许在长期任务前端里重复发明第二套状态结构、第二套时间线解析、第二套 capabilityPlan 读取逻辑。
- 不允许为了做一个新视图，就把读取、缓存、渲染、事件绑定全部内联到单个大型函数里。

#### 16.7.3 后续长期任务前端代码组织规则

从现在开始，凡是继续写入 `app.js` 的长期任务前端逻辑，都必须尽量遵循以下结构：

1. `parse/normalize` 层  
   负责把 `tasks.json / checkpoints.json / capability-plans.json / progress.md` 解析成统一前端结构。
2. `load/cache` 层  
   负责 `readSourceFile`、缓存、重载控制、并发去重。
3. `render` 层  
   只负责拼装 UI，不直接做数据读取。
4. `bind actions` 层  
   只负责按钮、跳转、提交绑定，不直接内联复杂业务判断。

也就是说，后续新增功能必须优先按 `parseX / loadX / renderX / bindX` 的形态落地，保持与当前 `goal detail / checkpoint / capabilityPlan / canvas context` 已形成的模式一致。

#### 16.7.4 物理隔离要求

虽然暂不拆文件，但从现在开始，所有长期任务前端新增逻辑都应满足“物理隔离可见”：

- 用明显的大段分隔注释块隔开功能区，例如 `GOAL CAPABILITY`、`GOAL CHECKPOINT`、`CANVAS GOAL CONTEXT`。
- 同一功能的 `state / helper / render / bind` 尽量连续放置，减少跨区跳跃。
- 能放进局部 helper 的逻辑，不要继续堆到大型总函数内部。
- 新增复杂 UI 时，优先扩展现有 card / panel / context bar，不优先增加新的平行视图壳层。

#### 16.7.5 为未来 ES Modules 拆分预留边界

虽然当前不做正式拆分，但后续新增代码必须按未来模块边界思考。建议默认按下列“未来拆分目标”组织逻辑：

- `goal-data`：goal / task graph / checkpoint / capabilityPlan 的读取、缓存、解析
- `goal-detail`：goal detail 渲染与按钮绑定
- `goal-checkpoint-ui`：checkpoint modal、提交、审批动作
- `goal-capability-ui`：capabilityPlan 展示、偏差分析展示
- `canvas-goal-context`：Canvas 顶部长期任务上下文与 capabilityPlan 摘要

要求不是现在就新建这些文件，而是后续写进 `app.js` 的代码，必须尽量保持未来能沿这几个边界直接切出去。

#### 16.7.6 Phase 4 / Phase 5 的实施约束

从本方案进入 Phase 4 开始，前端开发必须遵守以下节奏：

- 先复用已有 UI 壳层，再新增展示能力；不要先造新页面再搬逻辑。
- 能挂进现有 `goal detail / canvas context / modal` 的能力，优先挂接，不先扩散为新总线。
- 每完成一个新的长期任务前端子功能，都要同步检查它是否复用了现有 `parse/load/render/bind` 结构。
- 每增加一个新的长期任务运行态文件读取入口，都必须先判断是否可并入现有 `goal data cache`，避免重复读取链路。

#### 16.7.7 触发正式拆分的阈值

虽然当前不拆分 `app.js`，但需要明确触发条件。一旦命中以下任一情况，应把“app.js 模块化拆分”从延后事项提升为当前任务：

- `app.js` 持续增长并明显逼近或超过 `10,000` 行。
- 长期任务相关代码块本身已经形成超过约 `1,500` 行的独立子系统。
- 同一前端功能在 3 个以上不相邻区域来回修改，维护成本明显升高。
- 出现因为全局状态污染、重复绑定、重复解析导致的实际回归或难以定位的前端故障。
- Phase 4 新增视图开始明显拖慢前端迭代节奏，出现“每加一个卡片都要改多个 unrelated 区域”的迹象。

#### 16.7.8 当前推荐策略总结

结合 `docs/app.js的技术债处理建议.md` 中的 3 条建议，当前推荐执行策略如下：

- 继续推进超长期任务系统，不因过早重构阻断主链路。
- 但从现在开始，所有长期任务前端实现都必须遵守“受控单体 + 模块就绪边界”的开发纪律。
- 在开发过程中，前端优先追求“核心基础功能可用 + 必要信息可见”，先保障功能正常运行与必要展示，暂不优先追求更完整、更精细的交互体验，以控制当前代码量增长速度。
- 在 Phase 4 与 Phase 5 期间，持续把新增逻辑压缩进可抽离块，直到正式进入 `app.js` 模块化拆分阶段。

这部分属于**预防性治理措施**。目标不是立即消灭技术债，而是避免技术债继续以失控方式增长，从而为后续正式拆分保留清晰边界。

---

## 17. 最终方案结论

Star Sanctuary 的超长期任务系统应定义为：

> 一个建立在 `methods + task memory + sub-agent orchestration + canvas + compaction + checkpoint` 之上的、以长期目标为中心的外部化执行操作系统。

它的核心不是再发明一个新 Agent，而是让现有 Agent 具备以下新能力：

- 能立项
- 能写蓝图
- 能拆 DAG
- 能按节点做能力规划
- 能在超长周期中恢复执行
- 能在关键点等待人审
- 能在结束后沉淀方法与技能

这就是 Star Sanctuary 下一阶段最值得建设的长期任务底座。

---

## 附录 A：Phase 1 开发任务拆解清单 + 文件级实现计划

### A.1 Phase 1 目标

Phase 1 先做最小可用底座，只解决四类核心问题：

1. 支持创建多个超长期任务
2. 支持默认路径 + 用户自定义 `goalRoot`
3. 支持独立 `goal session` 通道与 pause/resume
4. 支持生成标准文档脚手架
5. 支持把 `goalId/nodeId/runId` 注入 Task Memory

暂不做：

- 复杂 Goal Dashboard
- 完整 DAG 编排器
- 自动 skill / MCP 生成
- 复杂审批 UI

### A.2 开发任务拆解清单

#### P1-1 目标注册表与路径解析

目标：

- 建立长期任务注册中心
- 支持默认路径：`~/.star_sanctuary/goals/<goal-id>/`
- 支持自定义路径
- 统一注册表：`~/.star_sanctuary/goals/index.json`

任务：

- [ ] 设计 `GoalRegistryEntry` 数据结构
- [ ] 实现 `index.json` 读写、校验、原子写入
- [ ] 实现 `goalRoot/docRoot/northstarPath/runtimeRoot` 路径解析
- [ ] 实现默认路径与用户自定义路径切换逻辑
- [ ] 实现路径合法性/冲突检查

验收：

- [ ] 能注册多个 goal
- [ ] 能读取已有 goal
- [ ] 能识别自定义路径
- [ ] 写入注册表不覆盖其他 goal

#### P1-2 长期任务文档脚手架

目标：

- 一键生成长期任务标准文档目录与初始文件

任务：

- [ ] 生成 `~/.star_sanctuary/docs/long-tasks/<goal-slug>/`
- [ ] 生成以下文件：
  - [ ] `00-goal.md`
  - [ ] `01-target-solution.md`
  - [ ] `02-design.md`
  - [ ] `03-architecture-and-implementation.md`
  - [ ] `04-split-plan.md`
  - [ ] `05-task-list.md`
  - [ ] `NORTHSTAR.md`
  - [ ] `tasks.json`
  - [ ] `progress.md`
  - [ ] `handoff.md`
- [ ] `NORTHSTAR.md` 自动写入路径信息（含 `goalRoot`）

验收：

- [ ] 新 goal 创建后能自动生成完整文档树
- [ ] `NORTHSTAR.md` 中有目标路径声明
- [ ] `tasks.json` 初始结构合法

#### P1-3 长期任务运行态目录

目标：

- 建立每个 goal 的运行态目录

任务：

- [ ] 创建默认运行态目录：
  - [ ] `state.json`
  - [ ] `checkpoints.json`
  - [ ] `runtime.json`
  - [ ] `board-ref.json`
  - [ ] `runs/`
  - [ ] `artifacts/`
- [ ] 若配置了自定义 `goalRoot`，则运行态写入自定义路径
- [ ] 保证运行态与文档目录解耦

验收：

- [ ] 默认路径和自定义路径都能正常落盘
- [ ] 恢复时可通过注册表找到运行态

#### P1-4 长期任务专用通道（goal channel）

目标：

- 让长期任务与普通任务分通道

任务：

- [ ] 设计 session namespace：
  - [ ] `goal:{goalId}`
  - [ ] `goal:{goalId}:node:{nodeId}:run:{runId}`
- [ ] 实现 `goal_init`
- [ ] 实现 `goal_resume`
- [ ] 实现 `goal_pause`
- [ ] 恢复时从外部状态加载，而不是依赖旧聊天上下文
- [ ] goal tools 只在 goal channel 启用/提升优先级

验收：

- [ ] 可从普通聊天创建 goal
- [ ] 可恢复到指定 goal session
- [ ] pause 后 resume 能继续
- [ ] 普通任务不受影响

#### P1-5 Task Memory 注入 goal 元信息

目标：

- 把长期任务执行记录挂到现有 Task Memory 上

任务：

- [ ] `TaskRecord.metadata` 注入：
  - [ ] `goalId`
  - [ ] `nodeId`
  - [ ] `runId`
  - [ ] `goalSession`
- [ ] 在 `before_agent_start` / `after_tool_call` / `agent_end` 链路透传
- [ ] 保持普通任务无这些字段时仍正常工作

验收：

- [ ] goal 通道执行的任务可在 task record 中看到 `goalId`
- [ ] 普通任务无回归
- [ ] 后续可基于 metadata 扩展 goal 过滤

#### P1-6 基础查询与控制能力

目标：

- 先有最小可操作接口，便于后续前端接入

任务：

- [ ] 最小工具集：
  - [ ] `goal_init`
  - [ ] `goal_get`
  - [ ] `goal_list`
  - [ ] `goal_resume`
  - [ ] `goal_pause`
- [ ] 最小 RPC：
  - [ ] `goal.list`
  - [ ] `goal.get`
  - [ ] `goal.resume`
  - [ ] `goal.pause`

验收：

- [ ] 不依赖前端 UI 也可用
- [ ] 后续 WebChat/Canvas 可直接接

#### P1-7 测试与文档

目标：

- 保证多 goal 并存和路径切换不出事故

任务：

- [ ] 注册表测试
- [ ] 自定义路径测试
- [ ] 文档脚手架生成测试
- [ ] goal session 恢复测试
- [ ] Task Memory metadata 注入测试
- [ ] 普通任务回归测试

验收：

- [ ] 多 goal 并存通过
- [ ] pause/resume 通过
- [ ] 普通任务链路不回归

### A.3 文件级实现计划

#### `packages/belldandy-core`

新增文件：

- `src/goals/types.ts`
  - 定义 `LongTermGoal`、`GoalRegistryEntry`、`GoalPaths`、`GoalRuntimeState`
- `src/goals/paths.ts`
  - 解析默认路径、自定义 `goalRoot`、派生 `docRoot/northstarPath/tasksPath/progressPath/runtimeRoot`
- `src/goals/registry.ts`
  - 读写 `~/.star_sanctuary/goals/index.json`
  - goal 注册/更新/查询
- `src/goals/scaffold.ts`
  - 生成标准文档树与初始文件
- `src/goals/runtime.ts`
  - 初始化运行态目录与状态文件
- `src/goals/session.ts`
  - 生成/解析 `goal sessionKey` 与 `runId`

修改文件：

- `src/bin/gateway.ts`
  - 初始化 goal registry
  - 注册 goal tools / hooks
  - 控制 goal tools 的启用范围
- `src/server.ts`
  - 新增最小 goal RPC：`goal.list` / `goal.get` / `goal.resume` / `goal.pause`
- `src/server.test.ts`
  - 新增 goal RPC 与恢复链路测试

#### `packages/belldandy-skills`

新增文件：

- `src/builtin/goals/goal-init.ts`
- `src/builtin/goals/goal-get.ts`
- `src/builtin/goals/goal-list.ts`
- `src/builtin/goals/goal-resume.ts`
- `src/builtin/goals/goal-pause.ts`
- `src/builtin/goals/index.ts`

修改文件：

- `src/index.ts`
  - 导出 goal tools

#### `packages/belldandy-memory`

修改文件：

- `src/task-types.ts`
  - 补充 goal metadata 约定字段说明
- `src/task-processor.ts`
  - 允许透传 `goalId/nodeId/runId/goalSession`
- `src/store.ts`
  - 第一阶段继续复用 `tasks.metadata`
- `src/manager.ts`
  - 预留 goal-aware 查询 helper

#### `packages/belldandy-agent`

最小改动策略：

- Phase 1 尽量少改 agent 包
- goal prompt 注入优先走 core hook，不先深入 agent runtime

#### Web 端（可延后）

最小改动建议：

- `apps/web/public/app.js`
  - 预留 goal mode 状态位
- `apps/web/public/index.html`
  - 预留“长期任务 / 恢复长期任务”入口

### A.4 建议的提交拆分

1. `feat: add long-task goal registry and path resolver`
2. `feat: add long-task document scaffold and runtime bootstrap`
3. `feat: add goal channel init resume pause tools`
4. `feat: inject goal metadata into task memory`
5. `test: cover multi-goal registry and goal session recovery`

### A.5 Phase 1 实施顺序

1. `core/goals/types.ts`
2. `core/goals/paths.ts`
3. `core/goals/registry.ts`
4. `core/goals/scaffold.ts`
5. `core/goals/runtime.ts`
6. `skills/builtin/goals/*`
7. `gateway.ts` 注册
8. `server.ts` 最小 RPC
9. `memory` metadata 注入
10. 测试

### A.6 最小验证清单

功能验证：

- [ ] 创建一个默认路径 goal
- [ ] 创建一个自定义路径 goal
- [ ] `index.json` 中能看到两个 goal
- [ ] 两个 goal 文档目录互不冲突
- [ ] pause 后可 resume
- [ ] 普通聊天仍然正常

数据验证：

- [ ] `NORTHSTAR.md` 写入了路径声明
- [ ] `runtime.json` / `state.json` 正常落盘
- [ ] Task Memory 中可看到 `goalId`

回归验证：

- [ ] `method_*` 不受影响
- [ ] `skills_*` 不受影响
- [ ] `delegate_task / delegate_parallel` 不受影响
- [ ] 非 goal chat 流程不受影响

### A.7 当前实施进度（2026-03-20）

已完成：

- [x] `packages/belldandy-core/src/goals/*` 底座模块
- [x] `~/.star_sanctuary/goals/index.json` 注册表
- [x] 默认路径 + 自定义 `goalRoot` 解析
- [x] 长期任务文档脚手架生成
- [x] 长期任务运行态目录初始化
- [x] `goal:{goalId}` / `goal:{goalId}:node:{nodeId}:run:{runId}` session 解析
- [x] server 最小 RPC：`goal.create / goal.list / goal.get / goal.resume / goal.pause`
- [x] gateway 接入 `GoalManager`
- [x] Task Memory 注入 `goalId/nodeId/runId/goalSession`
- [x] `packages/belldandy-skills/src/builtin/goals/*` tools 落地
- [x] goal tools 注册到 gateway / skills 导出
- [x] `goal_*` tools 在非 goal session 默认不暴露、也不可执行
- [x] 新增底座测试并通过
- [x] 全量 TypeScript 构建通过
- [x] WebChat 最小“长期任务 / 恢复长期任务”入口
  - 侧边栏新增 `长期任务` 入口
  - 支持 `goal.list / goal.create / goal.resume / goal.pause`
  - 创建后可直接进入独立 goal channel
  - 普通聊天通道与长期任务通道保持分离
- [x] WebChat 第二阶段增强（轻量）
  - 新建长期任务改为表单式 modal，而不是 `prompt()`
  - goal detail 可直接打开 `00-goal.md` / `runtime.json` / `state.json`
  - Memory Viewer 的 task 视图支持按 `goalId` 过滤，形成“goal → tasks”追踪闭环
- [x] goal registry/runtime 持久化上次 active node/run 信息
  - `pause` 后保留 `lastNodeId / lastRunId / lastActiveAt`
  - WebChat 可直接“按上次节点恢复”
  - `packages/belldandy-core/src/goals/manager.test.ts` / `packages/belldandy-core/src/server.test.ts` 已覆盖并通过
- [x] WebChat goal detail 支持 checkpoint / node 级可视追踪（最小版）
  - 直接读取 `tasks.json` / `checkpoints.json`
  - 展示节点统计、checkpoint 统计、最近节点、最近 checkpoint
  - 若读取失败，会提示自定义路径需加入可操作区
- [x] WebChat goal detail 与 Canvas 打通最小联动
  - 直接读取 `board-ref.json`，展示 runtime board-ref / registry boardId / 绑定状态
  - goal detail 可一键“打开关联画布”或退回“查看画布列表”
  - 若主板不存在或打开失败，会自动回退到 Canvas 列表，不阻断普通 goal/detail 浏览
- [x] Canvas 中新增反向 goal / node 上下文入口
  - 画布顶部新增上下文条，展示当前 `Board / Goal / Node / Run`
  - 若当前会话是 `goal session`，可直接从 Canvas 回跳长期任务详情、任务视图或当前节点通道
  - 若当前画布仅按 `boardId` 匹配到 goal，也会展示最小上下文入口，不影响独立画布使用
- [x] Canvas 支持对当前 `activeNodeId` 的最小高亮
  - Canvas 渲染层接入当前 goal context
  - 按 `node.id / data.nodeId / data.goalNodeId / data.taskNodeId / data.ref.id` 做最小匹配
  - 命中后会增加 `ACTIVE` 标记与视觉高亮，便于从画布反查当前执行节点

进行中：

- [ ] Phase 2：task graph / checkpoint 细化
  - [x] `tasks.json` 升级为正式 `task_graph` 协议（`version: 2`）
  - [x] `task_graph` 读写归一化与 legacy v1 stub 兼容读取
  - [x] 节点状态机骨架与合法迁移约束
  - [x] 依赖未满足时禁止 `claim / in_progress`
  - [x] `GoalManager` 最小图操作 API：`read/create/update/claim/complete/block`
  - [x] goal tools：`task_graph_read/create/update/claim/complete/block`
  - [x] gateway / server 接入最小 task graph 能力面
  - [x] `progress.md` 自动追加节点/审批关键事件
  - [x] checkpoint 基础审批流：`request -> waiting_user/pending_review -> approve/validating -> reject/blocked`
  - [x] checkpoint tools：`goal_checkpoint_list/request/approve/reject`
  - [x] gateway / server 接入 checkpoint 能力面
  - [x] 状态专用工具入口：`task_graph_pending_review / task_graph_validating / task_graph_fail / task_graph_skip`
  - [x] checkpoint `expire / reopen / history`
  - [x] WebChat goal detail 的 checkpoint 操作入口与 progress 时间线视图
  - [x] WebChat checkpoint 操作面板升级为正式 modal
    - 移除 `prompt()` 交互
    - `approve / reject / expire / reopen` 统一走结构化面板提交
    - 面板内直接填写 `summary / note`，避免中断式输入
  - [x] checkpoint 面板补 reviewer / SLA / 审批人上下文
    - `checkpoints.json` 正式记录 `reviewer / reviewerRole / requestedBy / decidedBy / slaAt`
    - WebChat modal 可直接查看并编辑 reviewer / SLA / 发起人 / 审批人
    - checkpoint history 补 actor，上下文不再只靠 note 文本兜底
  - [x] build 通过
  - [x] `manager.test.ts` / `goal-tools.test.ts` 通过
  - [x] Phase 4 治理收尾：`goal.update` `areas` 语义集中协议化
    - `GoalUpdateReason / GoalUpdateArea / GoalUpdateEvent` 已统一到 goals 类型层
    - `reason -> areas` 改为由集中协议表推导，manager 关键流转不再分散手写 `areas`
    - 该项属于治理协议稳定化，不是新增前端交互功能，目标是为 Phase 5 自动化提供稳定事件语义
  - [x] `progress.md` 自动追加与节点执行摘要沉淀
  - [x] checkpoint 审批流与 node 状态联动

待后续继续：

- [ ] Phase 2 下一步：补 checkpoint 多轮审批策略（review chain / quorum / escalation）
- [ ] Phase 2 下一步：补 reviewer 目录、默认 reviewer 继承与 SLA 超时提醒
- [x] Phase 3 最小入口：capabilityPlan / orchestrate
  - [x] `capability-plans.json` 运行态文件
  - [x] `goal_capability_plan`：按节点生成 `single_agent / multi_agent / methods / skills / MCP / gaps`
  - [x] `goal_orchestrate`：执行前先生成/读取 plan，再落到 `claim`，并可选触发最小子代理委托
  - [x] `progress.md` 追加 `capability_plan_generated / node_orchestrated`
  - [x] `capabilityPlan.actualUsage` 回写闭环
    - 从当前 Task Memory 自动提取实际 `methods / skills` 使用记录
    - 从当前 task toolCalls 自动提取实际 `MCP server / toolNames` 使用记录
    - `goal_capability_plan / goal_orchestrate` 在保存 plan 时一并回写
  - [x] orchestrate 与 checkpoint 策略联动（最小版）
    - planner 为节点补充 `riskLevel + checkpoint policy`
    - `goal_orchestrate` 遇到高风险节点会自动将 node 标记为 `checkpointRequired`
    - 若已自动发起 checkpoint，则本轮暂不继续子代理委托，先进入审批通道
  - [x] capabilityPlan 在 WebChat goal detail 中可视化（最小版）
    - 直接读取 `capability-plans.json`
    - 展示当前重点 plan / 最近 plans / risk / checkpoint policy / gaps
    - 展示 `actualUsage.methods / skills / MCP / toolNames`
  - [x] capabilityPlan 计划/实际偏差分析与补建议（最小版）
    - `saveCapabilityPlan` 自动计算 `analysis.status / deviations / recommendations`
    - 当前先覆盖 `planned_but_unused / unplanned_but_used / delegation_gap / usage_untracked`
    - WebChat goal detail 可直接看到 alignment summary / deviations / suggestions
  - [x] checkpoint 多级策略与 capability risk policy 对齐（最小版）
    - `riskLevel -> checkpoint policy` 已分级映射为 `none / single / strict`
    - `request / approve / reject / expire / reopen` 会按 policy 强校验必填字段
    - `goal_orchestrate` 自动下发 `reviewerRole / requestedBy / SLA` 等风险策略上下文
    - checkpoint 运行态会保留 policy snapshot，避免后续审批脱离原始风险策略
    - 当前阶段的实现重点是“分级策略对齐”，不是完整审批工作流引擎
    - 当前已支持：
      - 单 checkpoint 的风险分级、字段约束、审批上下文继承
      - 节点执行前按 risk policy 自动进入 checkpoint
      - 审批记录按 policy snapshot 落盘，避免执行期策略漂移
    - 当前尚未支持：
      - `review chain`：串行多审批人链路，例如 `tech lead -> producer -> owner`
      - `quorum`：多人会签/最小通过人数，例如 `2/3 approve`
      - `escalation workflow`：SLA 超时后的自动升级、自动改派、自动通知
      - 条件化审批分支：根据节点类型、artifact 类型、失败原因动态切换审批路径
      - policy 模板中心：跨 goal 复用 reviewer 目录、默认审批模板、组织级策略继承
    - 因此当前状态应理解为：
      - 已建立 `capability risk policy -> checkpoint enforcement` 的最小闭环
      - 但尚未进入完整的“审批编排系统 / 审批工作流引擎”阶段
    - 后续若补完完整 workflow，建议优先顺序：
      - 1. `review chain` 与 reviewer 目录
      - 2. `quorum` 与多人审批状态机
      - 3. `escalation workflow` 与 SLA 超时处理
      - 4. policy 模板化与跨项目继承
  - [x] capabilityPlan 与 Canvas 的可视入口（最小版）
    - Canvas 顶部上下文条可显示当前 goal / node 对应的 `capabilityPlan` 摘要
    - 当前展示 `planId / executionMode / riskLevel / alignment`
    - 支持从 Canvas 直接打开长期任务详情、goal tasks、当前 goal/node 通道、`capability-plans.json`
    - 当前阶段仍为只读入口，不在 Canvas 内直接编辑 capabilityPlan
  - [x] build 通过
  - [x] `manager.test.ts` / `goal-tools.test.ts` 通过

Phase 3 后续继续：

阶段结论（2026-03-20）：

- 当前实现已可从 Phase 3 / Phase 4 主线切换到 Phase 5。
- 切换依据不是“所有可视化增强都完成”，而是：
  - `capabilityPlan` 已形成计划、执行、actual usage、偏差分析闭环
  - `handoff` 已形成自动刷新闭环
  - `goal.update` 已形成可被自动化依赖的稳定事件协议
- 因此前端应保持克制，后续优先建设经验沉淀自动化主线。

## 附录 B：Phase 5 最小任务清单（2026-03-20 起始）

### B.1 Phase 5 目标

Phase 5 第一轮不追求“全自动 method/skill 发布系统”，而是先完成最小闭环：

1. 系统可基于单个 goal 的执行结果自动生成 retrospective
2. 系统可自动提出 method / skill 候选建议
3. 系统可检测高频节点流程与可复用模式
4. 结果可落盘、可追踪、可人工审阅、可供后续工具继续处理

### B.2 Phase 5 最小任务拆解

#### P5-1 goal retrospective 生成器

目标：

- 基于 `NORTHSTAR.md / tasks.json / progress.md / handoff.md / capability-plans.json / checkpoints.json / Task Memory` 生成目标级复盘摘要

任务：

- [x] 定义 retrospective 数据结构
- [x] 实现 goal retrospective 聚合器
- [x] 输出机器可读结果与 Markdown 摘要
- [x] 写入 goal 运行态与文档目录

建议输出：

- `retrospective.json`
- `06-retrospective.md`

验收：

- [x] 可对单个已执行 goal 生成 retrospective
- [x] retrospective 包含目标完成度、关键节点、阻塞、审批、能力偏差、建议沉淀项
- [x] 已提供最小 RPC / tool 入口：`goal.retrospect.generate / goal_retrospect_generate`
- [x] 本轮未新增新的长期任务前端壳层

#### P5-2 method candidate 自动建议

目标：

- 从 retrospective + 节点执行记录中自动提取值得沉淀为 method 的流程候选

任务：

- [x] 提取高复用、高确定性、步骤稳定的节点流程
- [x] 生成 method candidate 结构化建议
- [x] 关联证据来源（goalId / nodeId / runId / artifact）
- [x] 先落盘为建议，不直接自动发布

建议输出：

- `method-candidates.json`
- `07-method-candidates.md`

验收：

- [x] 可列出 method 候选
- [x] 每条建议都能回溯证据来源
- [x] 不会直接污染正式 `methods/`
- [x] 已提供最小 RPC / tool 入口：`goal.method_candidates.generate / goal_method_candidates_generate`

#### P5-3 skill candidate 自动建议

目标：

- 识别“当前能力不足但反复出现”的执行缺口，生成 skill 候选建议

任务：

- [x] 基于 `capabilityPlan.gaps / analysis / actualUsage` 提取能力缺口模式
- [x] 识别重复出现的工具编排、外部依赖、人工步骤
- [x] 生成 skill candidate 建议与理由

建议输出：

- `skill-candidates.json`
- `08-skill-candidates.md`

验收：

- [x] 可输出 skill 候选清单
- [x] 可区分“值得做成 skill”与“只需补 method”的情况
- [x] 已提供最小 RPC / tool 入口：`goal.skill_candidates.generate / goal_skill_candidates_generate`

#### P5-4 高频流程检测

目标：

- 识别多个节点/多个 goal 中重复出现的流程模式，为 method/skill 沉淀提供排序依据

任务：

- [x] 定义最小 flow signature
- [x] 基于节点状态流转、tool usage、capability plan 生成 signature
- [x] 输出高频流程排行与建议动作

建议输出：

- `flow-patterns.json`
- `09-flow-patterns.md`

验收：

- [x] 至少能识别同一 goal 内重复流程
- [x] 输出可排序的高频流程摘要
- [x] 已提供最小 RPC / tool 入口：`goal.flow_patterns.generate / goal_flow_patterns_generate`

#### P5-5 最小工具与 RPC 接口

目标：

- 让 Phase 5 结果先能被系统调用和后续接 UI，而不是先做新页面

任务：

- [x] `goal_retrospect_generate`
- [x] `goal_experience_suggest`
- [x] `goal_flow_patterns_generate`
- [x] 最小 RPC：`goal.retrospect.generate / goal.experience.suggest / goal.flow_patterns.generate`

当前实现说明：

- `goal_experience_suggest` 已作为 Phase 5 聚合入口落地，复用现有 `retrospective / method-candidates / skill-candidates / flow-patterns` 生成器，不新增平行建议链路。
- 聚合结果统一返回 `retrospective + methodCandidates + skillCandidates + flowPatterns + summary + recommendations`，供后续系统调用或 WebChat 只读接入复用。
- 当前保持克制：不新增 Goal Dashboard、不新增第二套前端状态缓存、不直接写入正式 `experience_candidates` 池。

验收：

- [x] 不依赖新增前端页面也可使用
- [x] 后续 WebChat 如需接入，只需复用现有 goal detail 只读卡片或 modal

#### P5-6 前端接入约束

目标：

- 确保 Phase 5 不违反 `16.7` 的前端技术债预防策略

任务：

- [x] Phase 5 第一轮不新建 Goal Dashboard 二级页面
- [x] Phase 5 第一轮不新增第二套长期任务状态缓存
- [x] 若必须展示 retrospective / suggestions，优先在现有 goal detail 中以只读摘要卡片接入
- [x] 所有新增读取逻辑优先复用现有 goal data cache / parse-load-render-bind 结构

当前收口说明：

- Phase 5 第一轮实际以后端自动化闭环与 tool / RPC 可用性为主，本轮未新增新的长期任务前端展示壳层，因此前端约束通过“未扩散前端链路”的方式满足。
- 后续若补最小只读展示，也必须继续复用现有 `goal detail` 读取与刷新链路，不得引入独立长期任务缓存。

验收：

- [x] Phase 5 完成时 `app.js` 仅发生受控增量修改
- [x] 不出现新的长期任务前端壳层或平行数据链路

### B.3 Phase 5 建议实施顺序

1. `goal retrospective` 聚合器
2. method candidate 建议器
3. skill candidate 建议器
4. 高频流程检测
5. goal tools / RPC 接口
6. 如有必要，再补最小只读前端入口

### B.4 Phase 5 第一轮完成标准

- [x] 至少一个已执行 goal 可自动生成 retrospective
- [x] 至少可输出 method / skill 候选建议
- [x] 至少可输出一版高频流程摘要
- [x] 结果可被 goal tools / RPC 读取
- [x] 不依赖新 Dashboard 页面

第一轮收口说明：

- 本轮完成标准以“后端自动化经验产出闭环成立、结果可落盘与可读取、前端未扩散”为准，已满足进入后续阶段的条件。
- 本轮明确不包含：正式 method/skill 自动发布、跨 goal 全局经验池归并、复杂前端 dashboard 化体验。

## 附录 C：Phase 6 最小任务拆解清单 + 文件级实现计划

### C.1 Phase 6 目标

Phase 6 第一轮不追求“全自动发布与全组织经验平台”，而是先完成最小治理闭环：

1. goal 侧 suggestion 可进入正式审阅状态
2. method / skill 至少一种可人工批准后发布
3. 发布结果能回溯到 `goalId / nodeId / runId / evidence`
4. flow pattern 至少可做跨 goal 的最小归并摘要
5. 全过程尽量复用现有 `experience_candidates / methodCreate / publishSkillCandidate / goal tools / server RPC`

### C.2 最小任务拆解清单

#### P6-1 goal suggestion review state

目标：

- 为 goal 侧 `method-candidates / skill-candidates / flow-patterns` 增加正式审阅状态，形成“建议 -> 审阅中 -> 接受/拒绝/搁置/需修改”的最小状态机

任务：

- [x] 定义 goal suggestion review 数据结构
- [x] 为 method / skill / flow pattern 增加 review status、reviewer、decision note、timestamps
- [x] 新增 goal 侧 review state 运行态文件
- [x] 保留 suggestion 与原始证据、源文件路径、来源 goal/node/run 的映射

建议输出：

- `suggestion-reviews.json`
- `10-suggestion-reviews.md`

当前实现说明：

- 第一轮先落 `suggestion-reviews.json`，不急于生成 `10-suggestion-reviews.md`，避免在 Phase 6 起步阶段扩张文档渲染链路。
- review items 当前从现有 goal suggestion 文件自动同步生成，不另造平行 suggestion 数据源。

验收：

- [x] 至少 1 条 goal suggestion 可进入 review 状态
- [x] 可记录 accept / reject / defer / needs_revision
- [x] review 记录可回溯到原始 suggestion 与 goal 证据

#### P6-2 suggestion publish pipeline

目标：

- 把 goal suggestion 与现有正式资产发布能力接通，不再停留在建议层

任务：

- [x] method candidate -> 正式 method 发布映射
- [x] skill candidate -> 正式 skill 发布映射
- [x] 发布结果写入 `publishedPath / assetKey / reviewId / sourcePath`
- [x] 拒绝直接跳过人工审阅自动发布

建议输出：

- `publish-records.json`
- `11-publish-records.md`

当前实现说明：

- 第一轮先落 `publish-records.json`，不急于生成 `11-publish-records.md`，保持 Phase 6 前期以运行态治理为主。
- 当前发布前提是对应 suggestion review 已为 `accepted`；若 review 未通过，则禁止发布。
- 当前 method / skill 发布已接入正式资产目录，但尚未同步进入全局 `experience_candidates` 治理层，该部分保留到 P6-3。

验收：

- [x] 至少 method 或 skill 一种可从 goal suggestion 发布成功
- [x] 发布后能记录 `publishedPath / reviewer / sourceSuggestionId`
- [x] 不破坏现有 `experience_candidate_accept` 与 `publishSkillCandidate` 逻辑

#### P6-3 goal suggestion -> global experience 映射

目标：

- 把 goal suggestion 与已有 `experience_candidates` 体系接上，统一进入经验治理层

任务：

- [x] 定义 goal suggestion 到 experience candidate 的映射规则
- [x] 支持从 goal suggestion 生成或同步到 `experience_candidates`
- [x] 防止重复灌入：对同一来源做最小去重
- [x] 写入来源字段，避免进入全局池后失去 goal 追踪链

当前实现说明：

- 当前映射发生在“正式 publish 之后”，而不是 review 阶段；只有正式发布的 suggestion 才进入全局经验池。
- 当前未扩表新增复杂 source schema，而是先复用现有 `ExperienceCandidate.sourceTaskSnapshot` 与稳定 synthetic taskId 表达 goal 来源。
- 当前最小去重依赖现有 `experience_candidates(task_id, type)` 唯一约束，不额外引入更复杂的多字段归并逻辑。

验收：

- [x] goal suggestion 可进入全局 experience candidate 层
- [x] 同一来源不会重复产生多条等价候选
- [x] 全局 candidate 仍可回跳到 goal 来源

#### P6-4 cross-goal flow patterns

目标：

- 在 Phase 5 的单 goal flow summary 之上，补最小跨 goal 归并

任务：

- [x] 定义跨 goal flow signature 聚合键
- [x] 扫描多个 goal 的 `flow-patterns.json`
- [x] 输出重复模式排行、影响 goal 数、推荐动作
- [x] 保留 node/goal refs，便于审阅与发布时追证

当前实现说明：

- 当前采用“全局只读聚合器”方案：
  - 读取所有 goal 已生成的 `flow-patterns.json`
  - 按 `signature` 归并为跨 goal 模式
  - 输出到统一全局文件，而不是回写各个 goal 的运行态
- 当前输出：
  - `~/.star_sanctuary/goals/cross-goal-flow-patterns.json`
  - `~/.star_sanctuary/docs/long-tasks/cross-goal-flow-patterns.md`
- 当前 `recommendedAction` 规则：
  - 若已有更强动作票据，沿用 `promote_both / promote_skill / promote_method`
  - 若仅为保守票据，但同一 signature 已跨 `>= 2` 个 goal 重复出现，则最少提升为 `promote_method`
- 当前仍未做：
  - 不把跨 goal 归并结果直接发布为正式 method / skill
  - 不在这一轮接完整 review chain / quorum / escalation workflow
  - 不新增新的长期任务前端壳层

建议输出：

- `cross-goal-flow-patterns.json`
- `12-cross-goal-flow-patterns.md`

验收：

- [x] 至少能识别 2 个 goal 间重复 flow signature
- [x] 输出 `goalCount / occurrenceCount / recommendedAction`
- [x] 可作为后续 method/skill 发布排序依据

#### P6-5 review governance 最小入口

目标：

- 提供最小 review / publish 工具与 RPC，而不是先做复杂治理前端

任务：

- [x] 新增 goal suggestion review / publish tools
- [x] 新增最小 RPC：review、list review state、publish、cross-goal flow summary
- [x] 复用现有 secureMethods / GOAL_TOOL_NAMES / goal-only 能力注入策略
- [x] 如需前端可见性，仅补只读 review summary，不开新页面

当前实现说明：

- 当前已具备以下最小治理入口：
  - `goal.suggestion_review.list / goal_suggestion_review_list`
  - `goal.suggestion_review.decide / goal_suggestion_review_decide`
  - `goal.suggestion.publish / goal_suggestion_publish`
  - `goal.flow_patterns.cross_goal / goal_cross_goal_flow_patterns`
  - `goal.review_governance.summary / goal_review_governance_summary`
- 其中 `goal.review_governance.summary` 作为 Phase 6 第一轮的收口入口：
  - 聚合当前 goal 的 review 状态分布、类型分布、可执行项
  - 聚合当前 goal 已发布记录
  - 聚合与当前 goal 相关的跨 goal 高频流程 focus patterns
  - 输出最小 recommendations，供 CLI / tool / 后续只读 UI 复用
- 当前阶段仍坚持后端优先与前端克制：
  - 不新增 Dashboard
  - 不新增长期任务专用前端 store
  - 如需展示，仅复用现有 goal detail / modal 做只读接入

验收：

- [x] 不依赖新 Dashboard 页面也可完成 review / publish
- [x] WebChat 如需接入，只需复用现有 goal detail 或 modal

#### P6-6 review chain / quorum / escalation workflow 骨架

目标：

- 为 Phase 6 建立统一审批工作流内核，并先在 `suggestion review + checkpoint` 两条链路上收口首版治理闭环

任务：

- [x] 为 suggestion review 增加 workflow snapshot 数据结构
- [x] 支持 `single / chain / quorum` 的最小配置入口
- [x] 让 review decision 开始按 workflow 推进，而不是一律直接终态
- [x] 支持当前 stage 的手动 escalation
- [x] 补 reviewer 目录 / 组织级 template / reminder / SLA policy 运行态
- [x] 让 checkpoint 复用统一 workflow engine
- [x] 补统一 approval scan / reminder / notification 聚合
- [x] 在现有 goal detail / modal 中补统一审批可视面板

当前实现说明：

- 当前 workflow 已收口到两条链路：
  - `suggestion review`
  - `checkpoint`
- 当前统一支持：
  - `single`：与原有单审阅兼容
  - `chain`：串行 stage 推进，前一 stage 通过后才进入下一 stage
  - `quorum`：同 stage 多 reviewer 会签，达到 `minApprovals` 后才整体通过
- 当前新增最小入口：
  - `goal.suggestion_review.workflow.set / goal_suggestion_review_workflow_set`
  - `goal.suggestion_review.escalate / goal_suggestion_review_escalate`
  - `goal.suggestion_review.scan / goal_suggestion_review_scan`
  - `goal.approval.scan / goal_approval_scan`
  - `goal.checkpoint.escalate / goal_checkpoint_escalate`
- 当前已补 cron 结构化 approval scan：
  - `CronPayload` 已支持 `goalApprovalScan`
  - scheduler 可直接执行 approval scan，而不再必须走 Agent prompt
  - 已支持单 goal / 多 goal / 全量 goal 周期扫描
  - 即使未配置 Agent，结构化 approval scan cron 任务仍可运行；仅 `systemEvent` 类型继续依赖 Agent
- 当前 `goal.suggestion_review.decide / goal_suggestion_review_decide` 已从“直接改终态”升级为“记录本轮投票并按 workflow 计算下一状态”
- 当前 escalation 已从“纯手动触发骨架”推进到“手动 + SLA 扫描 + reminder notification 首版闭环”：
  - 可按 SLA 超时或 `force=true` 手动升级
  - 可通过 `goal.suggestion_review.scan` 或 `goal.approval.scan` 扫描 overdue stage
  - 若 workflow 已配置 `escalationReviewer` 且启用 `autoEscalate`，扫描时会自动追加 reviewer 并保留 escalation history
  - 已写入 `review-notifications.json`，形成 `sla_reminder / sla_overdue / auto_escalated` 通知事件记录
- 当前 notification 已形成“事件层 + 下发层”两层运行态：
  - 事件层：`<goal.runtimeRoot>/review-notifications.json`
  - 下发层：`<goal.runtimeRoot>/review-notification-dispatches.json`
  - 当前已支持 `goal_detail / goal_channel / reviewer_inbox / org_feed / im_dm / webhook` 六类 channel 的 materialize / outbox 记录
  - 当前 `im_dm / webhook` 仅写运行态 outbox，状态保持 `pending`，尚未直接接入外部 IM / webhook
- 当前 reviewer / template / policy 继承已形成运行态闭环：
  - 组织级配置文件：`~/.star_sanctuary/governance/review-governance.json`
  - 可继承默认 suggestion template、checkpoint risk template、approvalMode template、reminderMinutes、notificationChannels、notificationRoutes
- 当前前端仍遵守 `16.7`：
  - 不新增 Dashboard
  - 不新增长期任务专用前端 store
  - 仅在现有 `goal detail / modal` 内补 unified approval panel
  - 当前 panel 已可查看 dispatch count、recent dispatches 与 dispatch outbox runtime 文件
- 当前剩余缺口已降为次级深化项：
  - 组织级 reviewer / template / SLA policy 在线编辑

验收：

- [x] `chain` 不会因第一张通过票直接整体通过
- [x] `quorum` 需达到最小通过人数后才整体通过
- [x] escalation 会保留运行态痕迹并更新当前 reviewer / stage context
- [x] checkpoint 与 suggestion review 已共用同一套 workflow engine
- [x] 已可统一输出审批 pending / overdue / notification 摘要
- [x] 已在现有 goal detail 内完成最小可视治理面板接入

### C.3 文件级实现计划

#### C.3.1 `packages/belldandy-core`

目标：

- 承担 goal suggestion review、publish orchestration、cross-goal aggregation 的核心编排

建议改动文件：

- `packages/belldandy-core/src/goals/types.ts`
  - 增加 `GoalSuggestionReviewRecord`
  - 增加 `GoalSuggestionPublishRecord`
  - 增加 `GoalCrossFlowPatternRecord`
  - 增加 review/publish 相关 result types
- `packages/belldandy-core/src/goals/runtime.ts`
  - 新增 `read/writeGoalSuggestionReviews`
  - 新增 `read/writeGoalPublishRecords`
- `packages/belldandy-core/src/goals/progress.ts`
  - 增加 `suggestion_review_requested / suggestion_accepted / suggestion_rejected / suggestion_published`
- `packages/belldandy-core/src/goals/manager.ts`
  - 新增 review / publish / cross-goal summary 主方法
  - 负责从 goal suggestion 生成 review state
  - 负责调用 method/skill 发布路径
  - 负责写入 progress 与 goal.update
- `packages/belldandy-core/src/goals/*`
  - 新增建议模块：
    - `suggestion-reviews.ts`
    - `publish-records.ts`
    - `cross-goal-flow-patterns.ts`
- `packages/belldandy-core/src/server.ts`
  - 暴露新 RPC：
    - `goal.suggestion.review.list`
    - `goal.suggestion.review.decide`
    - `goal.suggestion.publish`
    - `goal.flow_patterns.cross_goal`
- `packages/belldandy-core/src/bin/gateway.ts`
  - 注册新 goal tools
  - 接 goal capabilities 注入
  - 保持 goal-only 暴露范围控制

#### C.3.2 `packages/belldandy-skills`

目标：

- 提供最小 tool 层与现有 method/skill 发布能力的复用入口

建议改动文件：

- `packages/belldandy-skills/src/types.ts`
  - 扩展 `GoalCapabilities`
  - 增加 review / publish / cross-goal summary 返回类型
- `packages/belldandy-skills/src/builtin/goals/shared.ts`
  - 增加 review state、publish result、cross-goal flow summary 的格式化函数
- `packages/belldandy-skills/src/builtin/goals/`
  - 新增：
    - `goal-suggestion-review-list.ts`
    - `goal-suggestion-review-decide.ts`
    - `goal-suggestion-publish.ts`
    - `goal-cross-goal-flow-patterns.ts`
- `packages/belldandy-skills/src/builtin/goals/index.ts`
  - 导出新 goal tools
- `packages/belldandy-skills/src/index.ts`
  - 导出新工具

复用点：

- method 发布尽量复用 `packages/belldandy-skills/src/builtin/methodology/create.ts`
- skill 发布尽量复用 `packages/belldandy-skills/src/skill-publisher.ts`
- 不新造第二套发布工具链

#### C.3.3 `packages/belldandy-memory`

目标：

- 承接 goal suggestion 与全局 `experience_candidates` 的映射和去重

建议改动文件：

- `packages/belldandy-memory/src/experience-types.ts`
  - 增加 goal suggestion source 字段
- `packages/belldandy-memory/src/store.ts`
  - 增加 goal suggestion source metadata 持久化
  - 增加按 `goalId / nodeId / suggestionType / signature` 去重支持
- `packages/belldandy-memory/src/manager.ts`
  - 提供从 goal suggestion 写入 experience candidate 的最小入口
- `packages/belldandy-memory/src/index.ts`
  - 导出新增 manager/store 能力

复用原则：

- goal suggestion 不直接绕开现有经验池 schema
- 能扩字段就扩字段，不再新造第三套“goal candidate 池”

#### C.3.4 `apps/web/public`

目标：

- 仅在必要时提供最小只读可见性，不扩张前端壳层

建议改动文件：

- `apps/web/public/app.js`
  - 如需接入，仅在现有 goal detail 中增加只读 review summary / publish summary 区块
  - 复用现有 `goal.update`、区域级刷新、goal data cache

约束：

- Phase 6 第一轮不是前端阶段
- 若后端 review / publish 工具已可用，则前端可延后
- 不新增 Goal Review Dashboard，不新增独立长期任务状态缓存

### C.4 建议实施顺序

1. `suggestion-reviews.json` 与 review 状态机
2. method / skill 发布映射
3. goal suggestion -> global experience 映射
4. cross-goal flow pattern 归并
5. review / publish tools 与 RPC
6. 如有必要，再补最小只读 review summary 入口

### C.5 最小验证清单

- [x] `corepack pnpm build`
- [x] `packages/belldandy-core/src/goals/manager.test.ts`
- [ ] `packages/belldandy-core/src/server.test.ts`
- [x] `packages/belldandy-skills/src/builtin/goals/goal-tools.test.ts`
- [ ] `packages/belldandy-skills/src/builtin/memory.test.ts`
- [ ] `packages/belldandy-memory/src/store.test.ts`
- [x] 至少 1 条 method 或 skill suggestion 完成 review -> publish
- [x] 至少 2 个 goal 可生成跨 goal flow summary

### C.6 第一轮完成标准

- [x] 已形成 goal suggestion 的正式 review 状态机
- [x] 已形成至少一种资产的人工批准后发布闭环
- [x] 发布资产可回溯原始 goal / node / run / evidence
- [x] 已形成跨 goal flow pattern 最小归并摘要
- [x] 未引入新的长期任务前端壳层
