# 摄像头功能改造

## Goal

为 Agent 提供一套相对通用、稳定、可扩展的摄像头调用方案，先覆盖当前最现实的浏览器回环场景，再为后续接入原生节点 / 外部设备 provider 留出统一接口。

补充目标：

- 必须把“本机只有外接摄像头设备”的桌面场景纳入正式方案，而不是只把它当成浏览器枚举的附带结果
- 最终需要支持本机外接 USB 摄像头 / 采集卡类设备的稳定调用
- Agent 看到的接口应保持统一，不能因为底层来自浏览器、桌面原生还是远端 node 而分裂成三套工具

## Why

当前仓库里已经有浏览器能力、截图能力和 `mirror.html`，但过去的摄像头调用更像一次性链路，不适合作为长期的 Agent 能力面：

- 设备发现、权限状态、采集状态没有统一 runtime 表达
- 参数层面缺少统一的 `facing / deviceId / width / height / fit / mirror`
- 工具层面缺少稳定的 `camera_list`
- 后续如果要接入 node / 原生摄像头 provider，会直接把实现绑死在浏览器路径上

## 参考输入

### 仓库现状

- `packages/belldandy-skills/src/builtin/multimedia/camera.ts`
- `packages/belldandy-skills/src/builtin/multimedia/camera-runtime.ts`
- `apps/web/public/mirror.html`

当前最适合先落地的路径，是通过浏览器打开镜像页，使用 `getUserMedia()` 建立视频流，再由 Agent 走元素级截图获取静态照片。

### OpenClaw 参考

`tmp/openclaw/openclaw-main/docs/nodes/camera.md` 给出的经验比较清晰：

- 能力面拆成 `camera.list` / `camera.snap` / `camera.clip`
- 参数统一包含 `facing`、`deviceId`、尺寸 / 时长等
- 明确权限 gating、前台约束、错误码和 payload 限制
- `camera.list` 先做设备发现，`camera.snap` / `camera.clip` 再做采集

这些点对 Star Sanctuary 也成立，只是我们的第一阶段 provider 不是原生 node，而是 `browser_loopback`。

另外，OpenClaw 的 `node.invoke + camera.list/snap/clip` 思路也说明了一点：

- Agent 不应该直接理解平台细节
- 平台差异应该落在 provider / node 适配层
- `camera.list` 必须先成为稳定能力，而不是仅靠临时拍照碰运气

### Context7 / MDN 参考

基于 `/mdn/content` 检索到的 Web Camera 主文档结论：

- 摄像头主入口应使用 `navigator.mediaDevices.getUserMedia()`
- 设备选择应同时支持 `facingMode` 偏好和 `deviceId` 精确选择
- 设备列表来自 `navigator.mediaDevices.enumerateDevices()`
- 必须显式处理 `NotAllowedError`、`OverconstrainedError` 等权限 / 约束失败
- 在浏览器里拍静态图时，`video.srcObject` + `canvas` / 元素截图是标准路径

## 目标能力面

### 对 Agent 暴露的工具

第一阶段：

- `camera_list`
- `camera_snap`

第二阶段：

- `camera_clip`
- 统一的摄像头诊断 / 权限状态输出

### 统一输入参数

- `facing`: `front | back`
- `deviceId`: 指定具体摄像头
- `deviceRef`: 面向长期使用的逻辑设备引用，后续优先于瞬时 `deviceId`
- `provider`: 显式指定 provider，避免多 provider 并存时路由不确定
- `nodeId`: 当 provider 为 `node_device` 时指定目标设备节点
- `width` / `height`: 期望采集分辨率
- `fit`: `cover | contain`
- `mirror`: 是否镜像预览
- `delayMs`: 拍摄前等待，用于对焦 / 曝光 / 页面稳定
- `readyTimeoutMs`: 等待 provider 进入 ready 的超时

`camera_clip` 额外补：

- `durationMs`
- `includeAudio`
- `format`

### 统一输出结构

- `provider`
- `mirrorUrl` 或 provider 会话标识
- `state`
- `device`
- 媒体产物路径
- 设备元信息
- 失败时的结构化错误信息

## 外接摄像头是一等场景

这部分不是可选优化，而是正式目标。

原因很直接：

- 很多桌面机器没有内置摄像头，只有外接 USB 摄像头
- 直播 / 录课 / 会议设备经常以外接摄像头或采集卡形式存在
- 浏览器 `deviceId` 不稳定、权限依赖强，不能作为外设接入的最终方案
- Agent 长期任务如果依赖“浏览器恰好还连着、恰好拿到那个 deviceId”，稳定性不够

因此最终方案必须支持：

- 枚举本机外接摄像头
- 用稳定设备引用重新找到该设备
- 在不依赖浏览器页面的情况下完成拍照 / 录像
- 将产物按统一 contract 返回给 Agent

## 方案设计

### 1. Provider Registry

摄像头能力不直接绑在某一个实现上，而是抽象成 provider：

- `browser_loopback`
- `native_desktop`
- `node_device`

统一对外暴露：

- `listDevices()`
- `captureSnapshot()`
- `captureClip()`
- `diagnose()`

这样后面即使接入手机节点、桌面原生摄像头，也不需要重写 Agent 的工具契约。

建议把 camera 能力做成和邮件 provider 类似的 registry，而不是继续把逻辑堆回单个 `camera-runtime.ts`。

### 1.1 Provider Contract 草案

建议抽成独立类型层，例如：

```ts
export type CameraProviderId =
  | "browser_loopback"
  | "native_desktop"
  | "node_device";

export type CameraDeviceSource =
  | "integrated"
  | "external"
  | "virtual"
  | "capture_card"
  | "unknown";

export type CameraDeviceDescriptor = {
  provider: CameraProviderId;
  deviceId: string;
  deviceRef: string;
  stableKey?: string;
  label: string;
  facing?: "front" | "back";
  source: CameraDeviceSource;
  transport: "browser" | "native" | "node";
  external: boolean;
  available: boolean;
  metadata?: Record<string, unknown>;
};

export type CameraProviderCapabilities = {
  list: boolean;
  snapshot: boolean;
  clip: boolean;
  audio: boolean;
  hotplug: boolean;
  background: boolean;
};

export interface CameraProvider {
  readonly id: CameraProviderId;
  diagnose(context: CameraProviderContext): Promise<CameraProviderDiagnostic>;
  listDevices(input: CameraListRequest, context: CameraProviderContext): Promise<CameraListResponse>;
  captureSnapshot(input: CameraSnapshotRequest, context: CameraProviderContext): Promise<CameraSnapshotResponse>;
  captureClip?(input: CameraClipRequest, context: CameraProviderContext): Promise<CameraClipResponse>;
}
```

这里最关键的不是字段多，而是两点：

- `provider` 和 `deviceRef` 必须进入统一 contract
- `external` / `source` 必须被显式表达，否则外接设备永远只是“某个 label”

### 1.2 DeviceRef 设计

不能只靠浏览器或系统临时生成的 `deviceId`。

建议区分：

- `deviceId`
  当前 provider 会话内可直接调用的瞬时 ID
- `deviceRef`
  对 Agent 暴露的逻辑设备引用
- `stableKey`
  provider 内部用于设备再识别的稳定键

推荐格式：

- `browser_loopback:rear-default`
- `native_desktop:usb-logitech-brio`
- `node_device:<nodeId>:front`

本机外接摄像头优先依赖 `stableKey + label + hardwarePath` 组合做稳定识别，再映射为对 Agent 友好的 `deviceRef`。

### 2. Browser Loopback Runtime

第一阶段先落浏览器 provider，原因是：

- 项目已经具备浏览器接线和截图能力
- 不需要新增本地原生依赖
- 调试链路短，适合先打通 Agent 侧体验

运行链路：

1. `camera_list` / `camera_snap` 归一化参数
2. runtime 生成镜像页 URL
3. BrowserManager 复用或打开 `mirror.html`
4. 镜像页通过 `getUserMedia()` 建流
5. 镜像页通过 `window.__BELLDANDY_CAMERA_MIRROR__` 暴露状态桥
6. runtime 等待 `ready`
7. `camera_list` 读取设备与状态
8. `camera_snap` 对视频元素截图并落盘

它仍然保留价值，但应该明确定位为：

- 快速可用 provider
- Web 权限受控 provider
- 不是最终唯一 provider
- 不负责解决“只有外接摄像头且浏览器不稳定”这一类长期问题

### 2.1 Browser Provider 的边界

更明确地说，`browser_loopback` 适合：

- 已经连接浏览器
- 需要快速拍一张图
- 允许浏览器弹权限框

不适合作为最终主方案的场景：

- 长时间后台驻留
- 设备被浏览器 / 会议软件争抢
- 外接摄像头需要稳定重连
- 无浏览器或浏览器会话不可控

### 3. Mirror Page 责任边界

`apps/web/public/mirror.html` 只负责浏览器侧的媒体生命周期：

- 解析 URL 参数
- 调用 `getUserMedia()`
- 维护当前 stream
- 暴露设备列表
- 响应 `devicechange`
- 输出 `booting / requesting-permission / ready / error`

镜像页不直接承担 Agent 业务语义，业务语义放在 skills runtime。

### 4. Runtime State Model

统一状态对象至少包含：

- `status`
- `selectedFacing`
- `selectedDeviceId`
- `devices`
- `videoWidth` / `videoHeight`
- `settings`
- `error`
- `startedAt` / `updatedAt`

这样 Agent 拿到的不是“拍到了 / 没拍到”的二元结果，而是可解释的运行时状态。

建议进一步把 provider 诊断状态也纳入统一模型：

- `providerStatus`: `available | unavailable | degraded`
- `permissionState`
- `selectionReason`
- `fallbackChain`
- `lastSuccessfulCaptureAt`

### 5. 错误模型

错误分三层：

- provider 不可用：浏览器未连接、镜像页不可达
- 权限 / 设备失败：`NotAllowedError`、`NotFoundError`、`OverconstrainedError`
- 采集失败：ready 超时、视频元素缺失、截图失败

工具输出应尽量保留原始错误名和可读 message，避免只返回“拍摄失败”。

对外接摄像头，还需要额外覆盖：

- 设备被拔出
- 设备被其他进程独占
- 驱动异常或能力协商失败
- provider helper 未启动
- helper 已启动但无权限或未就绪

## 原生节点 / 外部设备方案

### 6. 目标 provider 列表

建议正式收敛为三类：

- `browser_loopback`
  当前已实现，适合浏览器回环
- `native_desktop`
  面向本机桌面摄像头，优先覆盖你的“只有外接摄像头”的场景
- `node_device`
  面向未来的手机 / Companion / 远端设备节点

### 6.1 `native_desktop` 的实现策略

不建议把平台原生摄像头采集代码直接塞进主 Gateway 进程。

更稳妥的做法是：

- 由主进程维护统一 camera contract
- `native_desktop` 通过本地 sidecar / bridge helper 实现
- helper 负责和操作系统摄像头 API 交互
- 主仓库只和 helper 通过 `stdio`、本地 HTTP 或 domain socket 通讯

这样做的好处：

- 平台差异被隔离，不污染主仓库
- 原生依赖、驱动问题、崩溃影响面更小
- 将来可以先做 Windows，再做 macOS / Linux
- 更符合仓库里现有 bridge / MCP / wrapper 的总体思路

### 6.2 为什么优先 sidecar / bridge，而不是主进程直连

对于本机外接摄像头，真正麻烦的不是“拍一张图”，而是：

- 设备热插拔
- 驱动和采集栈差异
- 进程独占与抢占
- 长时间运行时的资源释放
- 原生依赖对构建链的污染

sidecar 更适合承担这些责任：

- 枚举设备
- 生成稳定设备键
- 管理打开 / 关闭 / 重连
- 拍照 / 录像
- 输出文件路径和元信息
- 上报诊断状态

### 6.3 Windows 本机外接摄像头优先方案

考虑到你当前机器是 Windows，且明确依赖外接摄像头，建议实现顺序是：

1. 先定义 `native_desktop` provider contract
2. 先做 Windows helper
3. 优先支持外接 USB 摄像头拍照
4. 再扩到录像、音频、热插拔监听

实现手段上，文档层面建议保持开放，但工程上优先顺序建议是：

- 首选：专用 helper，内部使用平台摄像头 API
- 过渡方案：helper 内部包一层现成采集能力
- 不建议：让 Gateway 直接依赖某个 Windows 专有 capture 库

这样即使后面 helper 的内部实现从一种采集方式切换到另一种，Agent 侧 contract 也不需要变。

### 6.4 `node_device` provider 的定位

`node_device` 不是本机外接摄像头的替代，而是第二条能力线。

它适合：

- 手机 / 平板摄像头
- 远端 companion 设备
- 专门挂在局域网或工位上的采集节点

它的 contract 应该与 `native_desktop` 对齐，只是底层调用来自 `node.invoke` 或未来的设备桥接层。

### 6.5 Provider 选择策略

建议统一路由规则：

1. 用户显式传 `provider` 时，严格按指定 provider 走
2. 用户传 `deviceRef` 时，优先解析其 provider 前缀
3. 如果检测到本机 `native_desktop` 可用，且存在外接设备，桌面场景默认优先 `native_desktop`
4. 如果浏览器已连接且用户未指定 provider，可回退 `browser_loopback`
5. 远端设备或显式 `nodeId` 时走 `node_device`

核心原则：

- 设备选择尽量稳定
- provider 选择尽量可解释
- 回退链路必须可见，不能静默切 provider

### 6.6 外接设备识别策略

外接摄像头需要显式识别，否则系统换口、重启、驱动刷新后就会漂。

建议 provider 内部至少采集：

- friendly name
- hardware path / instance path
- vendor / product 信息
- 是否外接
- 是否虚拟设备
- 当前能力集

然后生成：

- `stableKey`
- `deviceRef`

这样 Agent 可以记住“会议室主摄像头”或“桌面 USB 摄像头”，而不是记住一次性的 `deviceId`。

### 6.7 Artifact Contract

不论底层 provider 是浏览器、桌面 helper 还是 node，都应该尽量统一产物行为：

- 图片 / 视频先落本地文件
- 返回路径、尺寸、格式、时长、设备信息
- 大媒体默认不直接内联进模型上下文
- 需要时再通过附件或后续工具读取

这点对外接摄像头尤其重要，因为视频或高分辨率图片很容易放大上下文成本。

## 当前实现落点

截至 `2026-04-17`，已经完成 `Phase 1`，完成 `Phase 3A` 的第一轮落地，并已完成 `Phase 3B` 的 helper 第一轮闭环与真实 Windows / Gateway smoke 验证；`Phase 3B` 第二轮已完成健康诊断 / 错误恢复可观测输出与最小 doctor 接面第一轮。

当前代码落点：

- `camera-runtime.ts` 作为浏览器摄像头 runtime
- `camera_list` 作为设备探测入口
- `camera_snap` 继续保留，并改成走统一 runtime
- `mirror.html` 暴露状态桥和设备列表
- 新增 camera contract / provider registry / `browser_loopback` provider
- 工具层已接入 `provider` / `deviceRef`
- 浏览器 provider 已开始回传 provider-aware 的设备描述和 `selectedDeviceRef`
- 已新增 `native_desktop` provider scaffold
- 已定义 Windows helper 的 `stdio` 协议消息结构和 helper client contract
- 已实现 `native_desktop` 的 `stdio` helper client，支持启动、握手、请求/响应、超时与基础 stderr 日志收集
- 已实现 Windows helper 端 `hello` / `diagnose` / `list_devices` / `capture_snapshot` runtime 与 `stdio` server entrypoint
- 当 helper 配置存在时，provider registry 已可注册 `native_desktop`
- 已在真实 Windows 外接摄像头环境下跑通 `native_desktop` 的 `camera_list` / `camera_snap` smoke
- 已在 Gateway WebSocket `message.send` 链路下跑通 `tool_call` / `tool_result` / `chat.final` 的真实工具调用闭环
- 已修复真实设备联调中暴露的 PowerShell 枚举脚本拼接错误、headerless DirectShow 设备解析、OBSBOT MJPEG 回退 `yuyv422`、helper idle auto-shutdown
- 已开始收敛 `Phase 3B` 第二轮：旧 `deviceRef` / `stableKey` 的重识别、`device_busy` 诊断与结构化 provider 诊断输出
- 已开始固化 helper 部署约束：启动前校验 `cwd` / 绝对路径 command / helper entry，收紧启动失败提示，并补充空闲自动回收环境变量
- 已补安装态 `runtimeDir/current` 回退解析：repo 相对 helper entry 现在可在源码仓库与安装态共用同一份 args 配置
- 已新增统一 `cameraRuntime` doctor report，并已接入 `bdd doctor`、`system.doctor` 与 Web doctor 最小卡片消费面
- doctor 现已开始展示 helper 的 `resolvedHelperEntry` / `runtimeDir`，用于定位安装态启动路径问题
- 已完成一轮真实 portable / install 产物 smoke 回归：修复 portable launcher 对 `@belldandy/protocol` 的运行时依赖解析、install 启动期 bridge sessions 源码直连，以及 portable smoke 固定 `28889` 端口导致的 `EADDRINUSE` 后，标准 `smoke:portable:full` / `smoke:install-script-lifecycle` 已通过
- 已补产物 runtime 级 `doctor` 缩窄验证，用于确认 camera helper 的安装态路径解析
- 已修复 install `current` Junction 布局下 helper 入口 main 判定过严导致的“helper 0 退出”，现在 `camera-native-desktop-helper` 会按 realpath 判断自身是否作为入口执行
- 已完成 camera helper 配置下的真实 portable / install 部署回归：portable 与 install 两种产物布局都已完成 `doctor + list + snap` 真机闭环

这意味着方案已经不是“文档 + 第一阶段浏览器骨架”，而是“文档 + 浏览器 provider 接入统一 contract 的多阶段骨架”。

但也要明确：

- 当前默认仍以 `browser_loopback` 作为 default provider；`native_desktop` 仅在 helper 配置存在时注册
- 当前 `native_desktop` 已完成 contract / protocol / `stdio` transport / provider scaffold / helper runtime
- 当前 `native_desktop` 已在本机真实外接摄像头上完成最小拍照闭环，并已完成占用恢复、拔掉后不误选、同口插回恢复；换口重识别因当前机器只有一个可用摄像头 USB 口，暂时无法做真机验证
- 当前还没有默认 provider 选择策略和 fallback 可观测；但 `native_desktop` 已开始向 `camera_list` / `camera_snap` 回传结构化 provider 诊断输出
- 当前机器上的真实 portable / install 产物 camera helper 配置回归已经完成；但跨机器 / 新鲜安装包回归、稳定 `deviceRef` 重识别和更完整的跨 provider 诊断输出仍未完成，这对整体方案来说仍是过渡状态，不是终态
- helper 部署约束已完成一轮源码仓库 + 安装态共享路径收敛，但仍缺少一轮真实跨机器 / 新鲜安装包回归
- 标准 `smoke:portable:full` / `smoke:install-script-lifecycle` 已通过；同时，camera helper 配置下的 portable / install 部署态 `doctor + list + snap` 也已在当前机器完成
- 作为缩窄验证：
  - portable full runtime 的 `bdd doctor --json` 已确认 `native_desktop.launchConfig.resolvedHelperEntry` 正确落到 `runtime/packages/belldandy-skills/dist/.../camera-native-desktop-helper.js`
  - install root 的 `bdd.cmd doctor --json` 已确认 `resolvedHelperEntry` 正确落到 `current/packages/belldandy-skills/dist/.../camera-native-desktop-helper.js`；此前 camera helper 配置下的 install root 缩窄验证出现的 `helper_unavailable` 已定位并修复为 Junction realpath 入口判定问题
  - portable full runtime 与 install current runtime 现都已完成部署态 `doctor + list + snap` 真机闭环，并已分别生成真实截图产物

已完成的代码落点可参考：

- `packages/belldandy-skills/src/builtin/multimedia/camera-contract.ts`
- `packages/belldandy-skills/src/builtin/multimedia/camera-provider-registry.ts`
- `packages/belldandy-skills/src/builtin/multimedia/camera-browser-loopback-provider.ts`
- `packages/belldandy-skills/src/builtin/multimedia/camera-runtime.ts`
- `packages/belldandy-skills/src/builtin/multimedia/camera.ts`
- `packages/belldandy-skills/src/builtin/multimedia/camera-native-desktop-contract.ts`
- `packages/belldandy-skills/src/builtin/multimedia/camera-native-desktop-protocol.ts`
- `packages/belldandy-skills/src/builtin/multimedia/camera-native-desktop-provider.ts`
- `packages/belldandy-skills/src/builtin/multimedia/camera-native-desktop-helper-runtime.ts`
- `packages/belldandy-skills/src/builtin/multimedia/camera-native-desktop-helper.ts`
- `packages/belldandy-skills/src/builtin/multimedia/camera-doctor.ts`
- `packages/belldandy-core/src/server-methods/system-doctor.ts`
- `packages/belldandy-core/src/cli/commands/doctor.ts`
- `apps/web/public/app/features/doctor-observability.js`

## 当前进度状态

### 已完成

- `Phase 1`：浏览器回环的 `camera_list` / `camera_snap`、参数归一化、ready 等待、镜像页状态桥、元素级截图落盘
- `Phase 3A`：camera provider types、registry / resolver、`browser_loopback` provider、工具层 `provider` / `deviceRef` 接口接入
- `Phase 3B`：`native_desktop` provider contract、Windows helper `stdio` 协议消息结构、provider scaffold、`stdio` helper client transport、helper runtime / server
- 真实 Windows 外接摄像头 smoke：`camera_list` / `camera_snap` 已完成最小手测闭环
- Gateway 真实工具链路 smoke：`message.send -> tool_call/tool_result -> chat.final` 已完成闭环验证
- `Phase 3B` 第二轮第一批：旧 `deviceRef` / `stableKey` 的重识别、`device_busy` 分类、helper 空闲自动回收、结构化 provider 诊断输出
- `Phase 3B` 第二轮第二批：统一 `cameraRuntime` 健康诊断摘要、`bdd doctor / system.doctor / Web doctor` 最小接面
- `Phase 3B` 第二轮第三批：helper 启动解析兼容安装态 `runtimeDir/current` 布局，doctor 可见解析后的 helper entry / runtimeDir
- `Phase 3B` 第二轮第四批：已修复 portable launcher / install 启动链 blocker，标准 `smoke:portable:full` / `smoke:install-script-lifecycle` 已通过
- 浏览器 provider 已开始向统一输出结构靠拢，设备列表中不再只是瞬时 `deviceId`

### 进行中

- 将浏览器 provider 的输出继续向完整统一 contract 靠拢
- 继续收敛 Windows helper 的稳定 `deviceRef` / `stableKey` 重识别，处理设备重插、换 USB 口、helper 重启后的恢复
- 继续为 Windows helper 的错误恢复、占用诊断、健康诊断补更完整 runtime 行为
- 继续补跨机器 / 新鲜安装包场景下的 camera helper 部署回归
- 继续把 `cameraRuntime` 从“单 provider 诊断卡”收敛成更完整的 provider 级健康与恢复摘要
- 继续补 camera helper 配置下的跨机器回归

### 未完成

- `node_device` provider
- `camera_clip`
- 多 provider 默认选择策略与 fallback 可观测
- 更完整的 camera 诊断 UI / 历史统计 / provider fallback 视图

## 分阶段落地

### Phase 1

目标：浏览器回环可用

- `camera_list`
- `camera_snap`
- 参数归一化
- ready 等待
- 镜像页状态桥
- 元素级截图落盘

状态：已完成。

### Phase 2

目标：把体验做顺

- 增加 `camera_clip`
- 设备复用与 stream restart 策略
- 更明确的诊断信息
- 更好的截图文件命名和产物管理

状态：未开始，保持 `defer`。

### Phase 3

目标：从“浏览器专用”变成“多 provider”

- 引入 camera provider registry
- 将 `camera-runtime.ts` 拆成 contract / registry / provider 实现
- 接入 `native_desktop` provider
- 接入 `node_device` provider
- 支持 provider 级 capability 探测和优先级选择

状态：已启动，当前完成到 `Phase 3B` 的 helper 第一轮闭环。

### Phase 3A

目标：先把接口立住

- 新增 camera provider types
- 新增 registry / resolver
- 将现有浏览器 runtime 改造成 `browser_loopback` provider
- 在工具层接入 `provider` / `deviceRef`

状态：已完成第一轮实现。

已完成说明：

- 已新增独立 camera contract 类型层
- 已新增 camera provider registry，并默认注册 `browser_loopback`
- 已将浏览器调用链改造成 `browser_loopback` provider 入口
- `camera_list` / `camera_snap` 已支持 `provider` / `deviceRef`
- 已补充定向单测和包级编译验证

保留项：

- 浏览器 provider 的诊断输出还不完整
- 统一 contract 里为 `native_desktop` / `node_device` 预留的字段还未被真正消费
- 默认 provider 选择策略暂未进入工具层自动路由

### Phase 3B

目标：优先打通本机外接摄像头

- 实现 Windows `native_desktop` helper
- 支持设备枚举
- 支持外接摄像头拍照
- 生成稳定 `deviceRef`
- 支持 provider 诊断

这是最关键的一步，因为它直接解决“本机只有外接摄像头”的实际问题。

状态：已完成第一轮 helper 闭环，并已完成真实 Windows / Gateway smoke；第二轮已完成结构化诊断输出、启动前部署校验、健康诊断 / 错误恢复摘要、最小 doctor 接面、安装态 `runtimeDir/current` 启动路径收敛，以及 helper 入口在 install Junction 布局下的 realpath-aware 自启动修复。标准 `portable / install` 产物 smoke 已通过，camera helper 配置下的 portable / install 部署态 `doctor + list + snap` 也已在当前机器完成；剩余主要是跨机器 / 新鲜安装包回归。

建议下一步按这个顺序推进：

1. 继续补跨机器 / 新鲜安装包场景下的 camera helper 部署回归，确认不依赖当前源码仓库环境
2. 继续补 Windows helper 的健康状态和错误恢复可观测输出，避免 doctor 只剩静态摘要
3. 在具备换口测试条件后继续收敛跨端口 `deviceRef` / `stableKey` 重识别
4. 在稳定重识别收敛后，再进入默认 provider 选择

### Phase 3C

目标：补齐 node 路径

- 实现 `node_device` provider adapter
- 映射 `camera.list` / `camera.snap` / `camera.clip`
- 把 node 返回结构归一化到统一 artifact contract

状态：未开始，保持 `split_task`。

### Phase 3D

目标：让多 provider 真正可用

- 默认 provider 选择策略
- fallback 可观测
- 设备别名 / 常用设备记忆
- 最小诊断工具输出

状态：已启动第一轮，当前仅完成 `native_desktop` 的最小诊断摘要接面；默认 provider 选择与 fallback 可观测仍未开始。

### Phase 4

目标：可观测与治理

- 权限 / 失败统计
- provider 健康检查
- WebChat / doctor 中更完整的诊断面与历史统计
- 面向 Agent 的更稳定错误恢复提示

状态：已启动第一轮，当前已补最小 camera runtime 卡片；完整诊断 UI 仍保持 `defer`。

## 接下来 1-2 轮计划

近期计划不再继续扩浏览器特性，而是优先解决“本机外接摄像头”这条主线。

1. 继续补 camera helper 配置下的跨机器 / 新鲜安装包部署回归
2. 继续补健康诊断、占用恢复和错误恢复的可解释输出，让 `cameraRuntime` 不只停留在静态一次性摘要
3. 在具备硬件条件后补换口真机验证，继续收敛跨端口 `deviceRef`
4. 让 registry 能显式区分 `browser_loopback` 与 `native_desktop`，但暂不做隐式自动切换
5. 保持 `camera_list` / `camera_snap` 对现有浏览器路径兼容，避免为接原生 helper 破坏现有链路

## 兼容性与约束

- 第一阶段依赖浏览器已连接并允许镜像页访问摄像头
- `enumerateDevices()` 在未授权前可能拿不到完整 label，这属于浏览器标准行为
- 浏览器 provider 更适合静态拍照；`camera_clip` 需要额外考虑录制生命周期和文件落盘
- 截至 `2026-04-17`，默认注册的 provider 仍只有 `browser_loopback`
- 当前方案尚未引入新的原生依赖，优先保证仓库现有运行方式不被破坏

补充约束：

- Windows helper 当前协议选型为 `stdio`
- `native_desktop` 需要明确 helper 部署方式
- 如果 helper command 使用 `node` / `node.exe`，则 args 中必须包含 helper 入口脚本路径
- 若 `command` / `cwd` 使用路径形式，启动前会校验路径是否存在
- 已新增 `BELLDANDY_CAMERA_NATIVE_HELPER_IDLE_SHUTDOWN_MS`，用于控制 helper 空闲自动回收
- 已开始消费 `STAR_SANCTUARY_RUNTIME_DIR` / `BELLDANDY_RUNTIME_DIR`，用于安装态 `current/` 下的 helper entry 回退解析
- Windows 外接摄像头可能被会议软件独占，需要设计可解释错误
- `deviceRef` 必须考虑设备重插、换 USB 口、驱动刷新后的重识别
- 多 provider 并存时，默认选择逻辑不能让用户“以为拍的是 A，实际拍的是 B”

## Validation

### Unit

- 参数归一化测试
- 状态桥读取测试
- ready / error 等待逻辑测试
- provider registry 解析测试
- `native_desktop` deviceRef / selection 解析测试
- Windows helper 协议消息封装测试
- `native_desktop` provider scaffold 映射测试
- `native_desktop` `stdio` helper client 的启动 / 握手 / 超时 / 请求响应测试
- Windows helper runtime 的诊断 / 枚举 / 拍照测试
- Windows helper `stdio` 端到端测试（fake PowerShell / fake ffmpeg）

已完成：

- `node .\node_modules\vitest\vitest.mjs run packages/belldandy-skills/src/builtin/multimedia/camera.test.ts packages/belldandy-skills/src/builtin/multimedia/camera-runtime.test.ts packages/belldandy-skills/src/builtin/multimedia/camera-provider-registry.test.ts`
- `node .\node_modules\vitest\vitest.mjs run packages/belldandy-skills/src/builtin/multimedia/camera-native-desktop-contract.test.ts packages/belldandy-skills/src/builtin/multimedia/camera-native-desktop-protocol.test.ts packages/belldandy-skills/src/builtin/multimedia/camera-native-desktop-provider.test.ts`
- `node .\node_modules\vitest\vitest.mjs run packages/belldandy-skills/src/builtin/multimedia/camera-native-desktop-stdio-client.test.ts`
- `node .\node_modules\vitest\vitest.mjs run packages/belldandy-skills/src/builtin/multimedia/camera-native-desktop-stdio-client.test.ts --reporter verbose`
- `node .\node_modules\vitest\vitest.mjs run packages/belldandy-skills/src/builtin/multimedia/camera-native-desktop-helper-runtime.test.ts packages/belldandy-skills/src/builtin/multimedia/camera-native-desktop-helper.test.ts --reporter verbose`
- `node .\node_modules\vitest\vitest.mjs run packages/belldandy-skills/src/builtin/multimedia/camera-native-desktop-helper-runtime.test.ts packages/belldandy-skills/src/builtin/multimedia/camera-native-desktop-provider.test.ts packages/belldandy-skills/src/builtin/multimedia/camera-native-desktop-stdio-client.test.ts packages/belldandy-skills/src/builtin/multimedia/camera-native-desktop-helper.test.ts --reporter verbose`
- `node .\node_modules\vitest\vitest.mjs run packages/belldandy-skills/src/builtin/multimedia/camera.test.ts packages/belldandy-skills/src/builtin/multimedia/camera-runtime.test.ts packages/belldandy-skills/src/builtin/multimedia/camera-provider-registry.test.ts packages/belldandy-skills/src/builtin/multimedia/camera-native-desktop-contract.test.ts packages/belldandy-skills/src/builtin/multimedia/camera-native-desktop-protocol.test.ts packages/belldandy-skills/src/builtin/multimedia/camera-native-desktop-provider.test.ts packages/belldandy-skills/src/builtin/multimedia/camera-native-desktop-stdio-client.test.ts packages/belldandy-skills/src/builtin/multimedia/camera-native-desktop-helper-runtime.test.ts packages/belldandy-skills/src/builtin/multimedia/camera-native-desktop-helper.test.ts --reporter verbose`
- `node .\node_modules\vitest\vitest.mjs run packages/belldandy-skills/src/builtin/multimedia/camera-doctor.test.ts --reporter verbose`
- `node .\node_modules\vitest\vitest.mjs run packages/belldandy-skills/src/builtin/multimedia/camera-native-desktop-stdio-client.test.ts packages/belldandy-skills/src/builtin/multimedia/camera-doctor.test.ts --reporter verbose`
- `node .\node_modules\vitest\vitest.mjs run packages/star-sanctuary-distribution/src/runtime-paths.test.ts --reporter verbose`
- `node .\node_modules\vitest\vitest.mjs run packages/belldandy-core/src/bridge-subtask-runtime.test.ts --reporter verbose`
- `corepack pnpm --filter @star-sanctuary/distribution build`
- `node .\node_modules\vitest\vitest.mjs run packages/belldandy-core/src/cli/commands/doctor.test.ts --reporter verbose`
- `node .\node_modules\vitest\vitest.mjs run packages/belldandy-core/src/server.doctor.test.ts -t "system.doctor exposes camera runtime summary when native_desktop helper is configured" --reporter verbose`
- `node .\node_modules\vitest\vitest.mjs run apps/web/public/app/features/doctor-observability.test.js --reporter verbose`
- `corepack pnpm --filter @belldandy/skills build`
- `corepack pnpm --filter @belldandy/core build`
- `corepack pnpm build:portable:full`
- `corepack pnpm smoke:portable:full`
- `corepack pnpm smoke:install-script-lifecycle`
- portable runtime `camera-helper-runtime-check.mjs`：已完成真实部署态 `doctor + list + snap`
- install current runtime `camera-helper-runtime-check.mjs`：已完成真实部署态 `doctor + list + snap`

### Manual

- 启动 Gateway
- 连接浏览器
- 调用 `camera_list`
- 调用 `camera_snap`
- 验证：
  - 镜像页能拉起
  - 摄像头权限提示正常
  - 能看到设备列表
  - 截图文件真实落盘

状态：浏览器回环链路本轮未重新手测，需要在接入真实 `native_desktop` helper transport 前补一次。

补充：

- 已在真实 Windows 外接摄像头环境下完成 `native_desktop` smoke，成功枚举并拍摄 `OBSBOT Tiny 2 StreamCamera`
- 已完成“会议软件占用 -> `device_busy` -> 解除占用后恢复拍照”的真机验证
- 已完成“拔掉后不误选旧设备 -> 同口插回后恢复旧 `deviceRef`”的真机验证
- 当前机器只有一个可用于摄像头的 USB 口，因此“换口后重识别”暂时阻塞，保留为后续真机项
- 真实截图落盘示例：
  - `E:/project/star-sanctuary/screenshots/camera-native-desktop_2026-04-17T09-05-06-283Z.png`
  - `E:/project/star-sanctuary/screenshots/camera-native-desktop_2026-04-17T09-21-27-614Z.png`
  - `E:/project/star-sanctuary/screenshots/camera-native-desktop_2026-04-17T09-42-55-079Z.png`

### Integration

- 在仅有外接摄像头的 Windows 机器上验证 `native_desktop`
- 验证外接摄像头拔插前后 `deviceRef` 是否还能正确重映射
- 验证同一台机器上 `browser_loopback` 与 `native_desktop` 共存时的默认选择和显式选择
- 验证 `node_device` 返回结构是否和本机 provider 一致

已完成：

- Gateway WebSocket `message.send` 下的真实工具链路 smoke：
  - `message.send`
  - `tool_call(camera_list)`
  - `tool_result(camera_list)`
  - `tool_call(camera_snap)`
  - `tool_result(camera_snap)`
  - `chat.final`
- Gateway 会话 transcript 已写入最终 assistant 消息，确认调用层闭环成立
- 已完成“占用解除后恢复成功”的 Gateway 真实工具链路验证
- 已完成“拔掉后不误选旧设备、同口插回恢复成功”的工具层真机验证
- 已完成“安装根 cwd + runtimeDir=current + repo 相对 helper entry”的模拟安装布局回归
- 已完成 portable full runtime `bdd doctor --json` 缩窄验证，确认 `resolvedHelperEntry` 正确落到 portable runtime
- 已完成 install root `bdd.cmd doctor --json` 缩窄验证，确认 `resolvedHelperEntry` 正确落到 install `current/`
- 已完成 portable full runtime 的真实 `doctor + list + snap` 部署回归，确认 helper 配置在 portable 产物里可直接枚举并拍照
- 已完成 install current runtime 的真实 `doctor + list + snap` 部署回归，确认 Junction `current/` 布局下 helper 自启动、枚举和拍照都正常
- 已完成标准 `smoke:portable:full`，确认 portable launcher 不再被 `@belldandy/protocol` 依赖解析或固定端口冲突阻塞
- 已完成标准 `smoke:install-script-lifecycle`，确认 install 启动链不再被 bridge sessions 的源码路径导入阻塞

受限项：

- “换到另一个 USB 口后是否触发 `explicit_*_reidentified`”目前因硬件条件阻塞，暂未完成
- 跨机器 / 新鲜安装包回归尚未完成

### Regression Focus

- 不影响现有 browser 工具
- 不影响现有截图目录结构
- `camera_snap` 在未授权场景下能返回可解释错误
- `camera_list` / `camera_snap` 引入 `provider` / `deviceRef` 后仍保持旧调用兼容
- `CameraProviderCapabilities` 扩展出 `diagnose` 后不破坏已有 provider 接口
- `native_desktop` helper 在 Gateway 调用后不会遗留后台进程，空闲后能自动回收
- `cameraRuntime` doctor 摘要接面不应破坏既有 `bdd doctor` / `system.doctor` / Web doctor 输出
- 安装态 `runtimeDir/current` 下，repo 相对 helper entry 应能解析到真实 helper 入口

## 技术债决策

- `fix_now`: `Phase 1` 浏览器回环能力与 `camera_list`
- `fix_now`: `Phase 3A` 的 contract / registry / `browser_loopback` provider / `provider` / `deviceRef` 接口
- `fix_now`: `Phase 3B` 的 `native_desktop` provider contract、Windows helper `stdio` 协议、provider scaffold、`stdio` transport、helper runtime / stdio server
- `fix_now`: 把 `native_desktop` 和外接摄像头支持写进正式设计，不再停留在“以后再说”
- `fix_now`: `Phase 3B` 第二轮的旧 `deviceRef` / `stableKey` 重识别、`device_busy` 分类、helper 空闲自动回收、结构化 provider 诊断输出
- `fix_now`: `Phase 3B` 第二轮的统一 `cameraRuntime` 健康诊断摘要，以及 `bdd doctor / system.doctor / Web doctor` 的最小接面
- `fix_now`: `Phase 3B` 第二轮的安装态 `runtimeDir/current` 启动路径收敛，以及 doctor 中解析后的 helper launch 摘要
- `fix_now`: 这轮真实 portable / install smoke 暴露出的 launcher `@belldandy/protocol` 依赖解析、install 启动期 bridge sessions 源码直连，以及 portable smoke 固定端口冲突
- `fix_now`: install `current` Junction 布局下 helper 入口 main 判定过严导致的 0 退出，以及 camera helper 配置下的当前机器 portable / install 真部署回归
- `split_task`: 真实 Windows 热插拔联调、跨机器 / 新鲜安装包部署回归、进一步稳定 `deviceRef`、更完整的 provider 诊断、`node_device` provider 分阶段落地
- `defer`: `camera_clip`、默认 provider 自动选择、完整诊断 UI 与历史统计
- `record_only`: 更完整的设备别名记忆、健康统计和治理指标，等 provider 体系稳定后再补

说明：

- 这次没有顺手把 `Phase 2` 或 `Phase 3B` 一起做掉，避免在 provider 边界还未稳定时扩大改动面
- `Phase 3A` 完成后，技术债的主矛盾已经从“浏览器链路不统一”切换到“Windows 外接摄像头原生 provider 尚未落地”
- 当前 `Phase 3B` 的主矛盾已经从“helper 不存在”切换到“真实 Windows 设备联调、部署约束和稳定重识别尚未完成”
