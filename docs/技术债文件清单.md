# 技术债文件清单

## 说明

- 本清单按“项目内源码文件当前行数 `> 4000`”筛选。
- 扫描范围：`apps/`、`packages/`、`scripts/`、`config/` 下已纳入仓库的 `ts/tsx/js/jsx/mjs/cjs` 代码文件。
- 已排除常见产物目录，如 `dist`、`build`、`coverage`、`node_modules`、`generated`、`vendor`。

## 超大型代码文件

| 路径 | 文件名 | 功能简述 | 当前行数 |
|---|---|---|---:|
| `packages/belldandy-core/src/server.test.ts` | `server.test.ts` | Gateway 服务端总集成测试入口；覆盖 WebSocket 握手、`message.send`、`system.doctor`、subtask/goal/query runtime、渠道与观测能力等大量服务端行为验证。 | 10365 |
| `packages/belldandy-core/src/server.ts` | `server.ts` | Gateway 服务端主入口；承载 HTTP / WebSocket 接入、请求分发、消息发送、doctor/query runtime、记忆/工具/子任务/安全等核心服务编排。 | 6561 |
| `apps/web/public/app.js` | `app.js` | WebChat 前端主装配入口；集中初始化聊天、目标、子任务、记忆、设置、外发、附件、面板切换与多项前端 feature 的接线。 | 5713 |

## 阈值说明

- `packages/belldandy-core/src/bin/gateway.ts` 当前为 `3879` 行。
- 该文件已接近仓库里“大文件继续外移”的关注阈值，但本次不属于“超过 4000 行”的清单项。
